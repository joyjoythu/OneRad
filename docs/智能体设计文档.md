# OneRad 智能体设计文档

> 版本：2026-07-22 · 基于 `app/agent/`、`app/api/`、`skills/`、`frontend/src/` 实际代码反向提炼

---

## 1. 设计目标与原则

### 1.1 领域背景

影像组学（Radiomics）是从医学影像中高通量提取定量特征并建立预测模型的技术。一次完整的分析涉及多个步骤：

```
图像/mask配对 → 临床表解析 → 参数确认 → QC → 特征提取 → 特征筛选+建模 → 报告生成
```

传统做法需要研究者手动切换多个 Python 脚本，每一步都涉及参数判断。OneRad Agent 的目标是**让临床研究者用自然语言描述分析意图**，Agent 在理解项目上下文后自主规划并执行流程，同时在关键操作节点（文件修改、脚本执行、耗时计算）征得用户确认。

### 1.2 核心设计原则

| 原则 | 说明 | 在代码中的体现 |
|------|------|---------------|
| **安全可控** | 所有副作用操作均需用户确认；路径活动被沙箱限制 | `Sandbox.resolve()` 拒绝越界路径；`process_tool_calls` 中 `confirmation_pending` 阻止静默执行 |
| **人机协同** | 采用 LangGraph `interrupt()` 挂起-确认-恢复循环，而非事后通知 | `human_review` 节点调用 `interrupt()` 序列化中断状态；前端渲染对应审批面板；用户操作通过 `Command(resume=...)` 恢复 |
| **可观测** | 思考链、提取进度、子Agent 状态通过 SSE 实时推送 | `_publish_thinking` / `_publish_agent_progress` / `_publish_subagent` 三个旁路发布函数 |
| **上下文高效** | 子Agent 在隔离上下文中运行，只有结论回到主对话 | `_run_subagent` 独立图 + 独立 MemorySaver；`_SUBAGENT_RESULT_MAX_CHARS = 4000` |
| **热更新友好** | System prompt 每次从磁盘读取，编辑即生效 | `skills.py` 的 `load_skill()` 无缓存设计 |

---

## 2. 整体架构

### 2.1 系统层次总览

```
┌──────────────────────────────────────────────────────────────────┐
│                       前端 (Vue 3 SPA)                            │
│                                                                  │
│  AgentChat.vue ─── 消息列表 + 输入框 + 模型选择 + 自动审批开关     │
│  ApprovalPanel.vue ─ 文件计划 / 系统命令 / 脚本审批                │
│  RadiomicsPanel.vue ─ 配对确认 / 提取参数 / 分析参数审批           │
│  TodoPanel.vue ── 右侧步骤进度条                                  │
│  PlanEditor.vue ── 可编辑的文件操作计划表格                        │
│  ScriptPanel.vue ── 代码高亮 + 风险标记                           │
│  CommandPanel.vue ── 系统命令确认                                 │
│                                                                  │
│  Pinia Store (useAgentStore):                                    │
│    messages, interrupt, pending_*, todos, thinking,               │
│    radiomics_progress, subagentStatuses, busy, ...               │
└──────────────┬──────────────────────────────┬────────────────────┘
               │   REST API (JSON)            │   SSE (EventSource)
               │   POST/PUT/DELETE            │   GET /threads/{id}/events
               ▼                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                       FastAPI 后端                                │
│                                                                  │
│  /api/agent/threads  ── 对话 CRUD + 消息/确认/取消/停止           │
│  /api/sse/events     ── EventBridge (pub/sub + SQLite持久化)     │
│                                                                  │
│  内存状态：                                                       │
│    app.state.agent_api_keys[thread_id]   ── API密钥，不落库       │
│    app.state.agent_auto_approve[thread_id] ── 自动审批开关        │
│    app.state.active_agent_streams         ── 运行中线程集合        │
│    app.state.agent_stream_tasks[thread_id]─ 异步任务引用          │
└──────────────┬──────────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────────┐
│              LangGraph 状态图 (5 节点循环)                         │
│                                                                  │
│  call_llm ──(有tool_calls)──▶ process_tool_calls                  │
│    ▲                            │                                │
│    │              ┌─────────────┼─────────────┐                  │
│    │              │ 免确认       │ 需确认       │                  │
│    │              ▼             ▼             │                  │
│    │         ToolMessage    human_review      │                  │
│    │              │          (interrupt)      │                  │
│    │              │             │             │                  │
│    │              │        auto_confirm       │                  │
│    │              │             │             │                  │
│    │              ▼             ▼             │                  │
│    │         call_llm    execute_confirmed    │                  │
│    │                        │                 │                  │
│    └────────────────────────┴─────────────────┘                  │
│                                                                  │
│  AsyncSqliteSaver ── checkpoint 持久化（messages + state）        │
└──────────────┬──────────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────────┐
│                    DeepSeek API                                   │
│                                                                  │
│  Model: deepseek-v4-flash (默认) / deepseek-v4-pro               │
│  endpoint: https://api.deepseek.com/v1                           │
│  key feature: reasoning_content (思考链非标字段)                  │
└──────────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈详表

| 层 | 技术 | 版本/说明 |
|----|------|----------|
| Agent 框架 | LangGraph | `StateGraph` + `interrupt()` + `AsyncSqliteSaver` |
| LLM 调用 | 原生 OpenAI SDK | 绕过 LangChain ChatOpenAI（保留 `reasoning_content`） |
| LLM | DeepSeek V4 | `deepseek-v4-flash`（默认）/ `deepseek-v4-pro` |
| 后端 | FastAPI + Starlette | 异步路由 + SSE 流式响应 |
| 数据库 | SQLite | 项目元数据 + 对话元数据 + SSE 事件持久化 |
| 前端 | Vue 3 + Element Plus | Composition API + Pinia + Vue Router + Vite |
| 特征提取 | PyRadiomics | 从 GitHub 源码安装 |
| 建模 | scikit-learn | LASSO + LogisticRegression + StratifiedKFold |
| 报告 | python-docx + Matplotlib | Word 报告 + ROC/校准/DCA 曲线 |

### 2.3 部署架构

```
┌─────────────────────────────────────┐
│          Docker 容器                 │
│                                     │
│  多阶段构建:                         │
│    Stage 1: node:20 ── npm build    │
│    Stage 2: python:3.11-slim        │
│             ├── FastAPI (8000)      │
│             └── 静态文件 (frontend/dist) │
│                                     │
│  挂载卷:                             │
│    /app/data  ── SQLite + settings  │
│    项目目录   ── 用户数据分析工作区     │
└─────────────────────────────────────┘
```

---

## 3. LangGraph 状态机详解

### 3.1 图结构（5 节点 + 2 条件边）

```python
# app/agent/graph.py
def create_agent_graph(checkpointer=None):
    builder = StateGraph(AgentState)

    builder.add_node("call_llm", call_llm)             # 节点1: LLM推理
    builder.add_node("process_tool_calls", process_tool_calls)  # 节点2: 解析工具调用
    builder.add_node("human_review", human_review)      # 节点3: 人工审批
    builder.add_node("auto_confirm", auto_confirm)      # 节点4: 自动审批
    builder.add_node("execute_confirmed", execute_confirmed) # 节点5: 执行确认

    builder.add_edge(START, "call_llm")
    builder.add_conditional_edges("call_llm", should_continue, {
        "process_tool_calls": "process_tool_calls",
        "__end__": END
    })
    builder.add_conditional_edges("process_tool_calls", route_after_process, {
        "human_review": "human_review",
        "auto_confirm": "auto_confirm",
        "call_llm": "call_llm"
    })
    builder.add_edge("human_review", "execute_confirmed")
    builder.add_edge("auto_confirm", "execute_confirmed")
    builder.add_edge("execute_confirmed", "call_llm")

    return builder.compile(checkpointer=checkpointer)
```

### 3.2 完整流转路径

一次完整的工具调用生命周期经过以下 superstep：

```
Superstep 1: call_llm
  ├── 组装 SystemMessage (skills/ 加载) + 历史 messages
  ├── 注册 17 个工具（根据 allow_subagent/readonly 动态裁剪）
  ├── 流式调用 DeepSeek
  │   ├── 每个 delta chunk 的 reasoning_content → _publish_thinking (SSE旁路)
  │   └── 累积 content + tool_calls → 组装 AIMessage
  └── 输出: AIMessage (content + tool_calls + reasoning_content 挂 additional_kwargs)

Superstep 2: process_tool_calls (if has tool_calls)
  ├── 遍历每个 tool_call
  ├── 按工具名路由：
  │   ├── update_todo_list → 直接写入 state.todos，返回 ToolMessage
  │   ├── dispatch_subagent(explore) → 直接并行执行子Agent，返回 ToolMessage
  │   ├── 需确认工具 → 设置 pending_* + interrupt_type
  │   └── 免确认工具 → 直接执行，返回 ToolMessage
  └── 约束：同一轮至多一个 pending_* 非空（confirmation_pending 标志）

Superstep 3a: auto_confirm (if auto_approve=True)
  └── 直接输出 {"confirmed": True}

Superstep 3b: human_review (if auto_approve=False)
  └── interrupt({"type": ..., pending_*: ...})
      └── 图挂起，序列化中断状态到 checkpoint
      └── 等待外部 Command(resume={"action": "confirm"|"cancel"|"other"})

Superstep 4: execute_confirmed
  ├── 根据 interrupt_type 取对应的 pending_* 字段
  ├── if confirmed:
  │   ├── file_plan → execute_plan() 执行文件操作
  │   ├── system_command → Sandbox 限制下执行命令
  │   ├── python_script → execute_script_if_safe()（low直接运行/medium注入沙箱头）
  │   ├── radiomics_execution → FeatureAgent.run()（在独立线程中提特征）
  │   ├── radiomics_analysis → run_radiomics_cv_analysis()
  │   ├── feature_statistics → run_feature_statistics()
  │   └── subagent_dispatch → _run_subagents()（并行子Agent）
  │   └── 结果注入 {"executed": True, "note": "操作已执行完成..."}
  ├── else:
  │   └── {"cancelled": True} + 可选 HumanMessage
  └── _clear_interrupt() → 清空所有 pending_* 字段

Superstep 5: call_llm (循环回到步骤1)
```

### 3.3 Checkpoint 持久化

使用 LangGraph 的 `AsyncSqliteSaver`，每次 superstep 结束后自动将 `AgentState` 完整快照写入 SQLite。关键行为：

- **持久化内容**：`messages` 列表（含 `additional_kwargs.reasoning_content`）、`todos`、`model`、`project_path`、`base_url`
- **不持久化**：`api_key`（走 `RunnableConfig.configurable`）
- **interrupt 恢复**：`Command(resume=...)` 触发时，LangGraph 从 checkpoint 恢复状态并从中断点继续
- **容错**：`/stop` 后清理残留的 `interrupt_type` 和 `pending_*` 字段，防止前端误弹审批面板。若消息历史末尾有未应答的 `tool_calls`（缺少对应 `ToolMessage`），为每个缺失 id 补一条 `{"cancelled": true}` 的 ToolMessage，避免下次 LLM 调用 400

---

## 4. 状态设计

### 4.1 AgentState 完整字段语义

```python
# app/agent/state.py
class AgentState(TypedDict):
    # ═══════════════════════════════════════════════════════════
    # 对话核心
    # ═══════════════════════════════════════════════════════════
    messages: Annotated[list, add_messages]
    # LangChain 消息累积器。add_messages reducer 保证新消息追加而非覆盖。
    # 包含 HumanMessage / AIMessage / ToolMessage / SystemMessage 四种类型。
    # AIMessage.additional_kwargs 携带 reasoning_content（思考链全文）和
    # timestamp（UTC ISO 8601）。ToolMessage 的 tool_call_id 与 AIMessage
    # tool_calls[*].id 一一对应。

    project_path: str     # 当前项目根目录（绝对路径）
    base_url: str         # LLM API 地址，固定为 "https://api.deepseek.com/v1"
    model: str            # 当前会话选用的模型名。新消息可携带 model 覆盖此值

    # ═══════════════════════════════════════════════════════════
    # 安全凭证（设计上不持久化到 checkpoint）
    # ═══════════════════════════════════════════════════════════
    api_key: NotRequired[Optional[str]]
    # 仅兼容旧版 checkpoint。新线程通过 RunnableConfig.configurable.api_key
    # 传入，避免密钥写入 SQLite。

    # ═══════════════════════════════════════════════════════════
    # 中断状态——同一时刻至多一个非 None
    # ═══════════════════════════════════════════════════════════
    interrupt_type: Optional[str]
    # 8 种值之一：
    #   "file_plan"           — plan_file_operations
    #   "system_command"      — list_directory / find_files / get_file_info /
    #                           read_yaml / read_json / read_tabular_file /
    #                           update_yaml / create_json / update_json /
    #                           inspect_image_spacing / word_create / word_append
    #   "python_script"       — execute_python_script
    #   "radiomics_plan"      — discover_radiomics_pairs
    #   "radiomics_execution" — extract_radiomics_features
    #   "radiomics_analysis"  — run_radiomics_analysis
    #   "feature_statistics"  — run_feature_statistics
    #   "subagent_dispatch"   — dispatch_subagent (general 模式)
    # 额外的非中断交互：
    #   "choice"              — ask_user_choice（通过 pending_choice 字段承载，
    #                            不经过 human_review，前端选择面板独立处理）

    pending_plan: Optional[Dict[str, Any]]
    # 结构: {"tool_call_id": str, "plan": [{"action": "move"|"copy"|"rename"|"mkdir",
    #         "source": str, "target": str, "reason": str, "overwrite": bool}]}
    # plan 已由 validate_plan() 校验，所有路径已通过沙箱检查。

    pending_command: Optional[Dict[str, Any]]
    # 结构: {"tool_call_id": str, "_pending_tool": str, "args": {...}}
    # 前端 CommandPanel 按 _pending_tool 决定展示格式。

    pending_script: Optional[Dict[str, Any]]
    # 结构: {"tool_call_id": str, "code": str, "description": str,
    #         "risk_level": "low"|"medium"|"high", "script_path": str}

    pending_radiomics_plan: Optional[Dict[str, Any]]
    # 结构: {"tool_call_id": str, "images_found": int, "masks_found": int,
    #         "pairs": {"high": [...], "medium": [...], "low": [...]},
    #         "unmatched_images": [...], "unmatched_masks": [...]}

    pending_radiomics_execution: Optional[Dict[str, Any]]
    # 结构: {"tool_call_id": str, "pairs": [...], "n_cases": int,
    #         "yaml_path": str, "output_dir": str, "expected_outputs": [...]}

    pending_radiomics_analysis: Optional[Dict[str, Any]]
    # 结构: {"tool_call_id": str, "feature_csv": str, "clinical": str,
    #         "id_col": str, "label_col": str, "covariates": [...],
    #         "output_dir": str, "n_feature_cases": int, "n_matched": int, ...}

    pending_feature_statistics: Optional[Dict[str, Any]]
    # 结构: {"tool_call_id": str, "feature_csv": str, "clinical": str,
    #         "selected_features_csv": str, "selected_features": [...], ...}

    pending_subagent: Optional[Dict[str, Any]]
    # 结构: {"tool_call_id": str, "tasks": [str, ...], "mode": "general"}

    pending_choice: Optional[Dict[str, Any]]
    # 结构: {"tool_call_id": str, "question": str, "options": [str, ...]}

    # ═══════════════════════════════════════════════════════════
    # 用户反馈
    # ═══════════════════════════════════════════════════════════
    confirmed: Optional[bool]            # True=确认 / False=取消
    choice_answer: Optional[str]         # 用户在选择面板提交的答案
    other_instruction: Optional[str]     # 用户选择"其他"时输入的替代指令

    # ═══════════════════════════════════════════════════════════
    # UI 辅助字段
    # ═══════════════════════════════════════════════════════════
    context_usage: Optional[Dict[str, Any]]
    # {"input_tokens": int, "output_tokens": int, "total_tokens": int}
    # 最近一次 LLM 调用的 token 用量（从 AIMessage.usage_metadata 提取）

    todos: NotRequired[List[Dict[str, str]]]
    # [{"content": "步骤描述", "status": "pending"|"in_progress"|"completed"}]
    # 由 update_todo_list 工具全量替换，前端 TodoPanel 渲染

    tool_outputs: Annotated[list, lambda x, y: (x or []) + y]
    operation_log: Annotated[list, lambda x, y: (x or []) + y]
    # 累积型列表字段。operation_log 用于前端状态栏，tool_outputs 用于日志面板
```

### 4.2 关键约束：同一轮只允许一个需确认的工具调用

LLM 可能在单次 `call_llm` 响应中并行发出多个 `tool_calls`（例如同时 `list_directory` 和 `read_yaml`）。虽然在 API 层面已设置 `parallel_tool_calls=False`，但工具定义层面仍需防御。

`process_tool_calls` 中的处理逻辑：

```
for each tool_call:
    if name in 需确认集合:
        if confirmation_pending:
            → 返回 error ToolMessage（保证每个 tool_call_id 都有响应）
            → continue
        confirmation_pending = True
        设置对应的 pending_* + interrupt_type
```

**设计理由**：如果允许多个 pending 状态，第二个会覆盖第一个的 `pending_*` 字段，导致第一个的 `tool_call_id` 永远收不到 ToolMessage，下次 LLM 调用时历史中 assistant 的 tool_calls 缺少对应 tool 消息，API 返回 400。

### 4.3 API Key 的不落库策略

```python
# _resolve_api_key 的三级优先级：
# 1. RunnableConfig.configurable.api_key  （运行时注入，不持久化）
# 2. state.api_key                          （兼容旧 checkpoint）
# 3. 环境变量 OPENAI_API_KEY / DEEPSEEK_API_KEY
```

创建和恢复线程时，前端传回的 API Key 存储在 `app.state.agent_api_keys[thread_id]`（FastAPI 内存字典），在 `_agent_config()` 中注入 `RunnableConfig`。线程删除时同步清理。

---

## 5. 工具体系

### 5.1 完整工具目录

#### 5.1.1 只读探索工具

| 工具 | 参数 | 返回值 | 确认 | readonly |
|------|------|--------|------|----------|
| `list_directory` | `path: str` | `{"_pending_tool": "list_directory", "args": {"path": ...}}` | 需要 | ✅ |
| `find_files` | `pattern: str`, `path: str = "."` | `{"_pending_tool": "find_files", "args": {...}}` | 需要 | ✅ |
| `get_file_info` | `path: str` | `{"_pending_tool": "get_file_info", "args": {...}}` | 需要 | ✅ |
| `read_yaml` | `path: str`, `key: str = ""` | `{"_pending_tool": "read_yaml", "args": {...}}` | 需要 | ✅ |
| `read_json` | `path: str`, `key: str = ""` | `{"_pending_tool": "read_json", "args": {...}}` | 需要 | ✅ |
| `read_tabular_file` | `path: str`, `sheet_name: str = ""`, `head: int = 20`, `columns: List[str] = None` | `{"_pending_tool": "read_tabular_file", "args": {...}}` | 需要 | ✅ |
| `discover_radiomics_pairs` | 无 | `{"_pending_tool": "discover_radiomics_pairs", ...}` | 需要 | ✅ |
| `inspect_image_spacing` | `pairs: List[Dict] = None` | `{"_pending_tool": "inspect_image_spacing", "args": {...}}` | 需要 | ✅ |

**read_tabular_file 的智能预处理**：自动尝试 UTF-8/GBK 编码；`columns` 可只读指定列；`head=0` 时只返回结构不取数据；结果 JSON 限制在 ~12KB 以内（超宽表时逐半减半 head_rows）。

**discover_radiomics_pairs 的配对算法**：递归扫描 `images/` 和 `masks/` 目录的 `.nii.gz` 文件 → 按文件名后缀匹配（`_mask`/`_seg`/`_label`/`_roi`）→ 按 token 交集匹配 → 按患者 ID 前缀匹配 → 输出高/中/低置信度三级配对结果。

#### 5.1.2 写入配置工具

| 工具 | 参数 | 说明 |
|------|------|------|
| `update_yaml` | `path: str`, `updates: Dict[str, Any]` | 点号路径到值的映射，如 `{"setting.binWidth": 10}`。保留注释与格式（`ruamel.yaml`），中间层级自动创建 |
| `create_json` | `path: str`, `content: Any` | 在项目内新建 JSON 文件（dict 或 list），格式化写入（缩进 2 空格、保留中文）。目标文件已存在时报错不覆盖，父目录自动创建 |
| `update_json` | `path: str`, `updates: Dict[str, Any]` | 点号路径到值的映射，如 `{"setting.binWidth": 10}`；值传 `null` 表示删除该键。缺失的中间层级自动创建为对象；目标文件必须已存在且是合法 JSON |

#### 5.1.3 计划与执行工具

| 工具 | 参数 | 说明 |
|------|------|------|
| `plan_file_operations` | `instruction: str` | 调用 LLM（独立实例）生成文件操作计划 → `validate_plan()` 校验。只允许 move/copy/rename/mkdir 四种操作。实际执行在确认后走 `execute_plan()`（含自动备份到 `.onerad_backup/`） |
| `execute_python_script` | `description: str`, `code: str` | 保存脚本到 `agent_scripts/` 目录 → AST 风险分级 → 确认后执行。low 直接运行，medium 注入沙箱头后运行，high 经用户知情确认后运行 |

**Python 脚本风险分级的 AST 分析**（`classify_risk()`）：

```
高危判定 (high):
  - import socket / subprocess / ctypes / urllib / requests / httpx / ...
  - from os import system / from shutil import rmtree
  - 调用 eval() / exec() / os.system() / subprocess.Popen()
  - 字符串中出现 __builtins__ / __import__

中危判定 (medium):
  - 导入了既非高危也非白名单的模块（如 numpy, pandas, nibabel）
  - 写文件操作（open(..., 'w')）
  - 路径遍历模式（'../'）
  - 绝对路径引用
  - 调用 remove() / unlink() / getattr() / Process()

低危 (low):
  - 仅使用白名单模块（math, json, csv, re, collections, ...）
  - 无写操作、无网络、无子进程
```

**沙箱头注入**（仅 medium 脚本）：在脚本开头注入 Hook 代码，运行时替换 `builtins.open` 和 `io.open`，对越界访问抛出 `PermissionError`。注意这是 best-effort 防御，不是安全边界（无法防护系统调用和直接使用 `os` 模块）。

#### 5.1.4 影像组学工具

| 工具 | 参数 | 核心逻辑 |
|------|------|---------|
| `extract_radiomics_features` | `pairs: List[Dict]`, `yaml_path: str = ""`, `force_rerun: bool = False` | 校验 YAML 存在性 → 校验全部路径在沙箱内 → 返回待确认元数据（n_cases, output_dir, expected_outputs）。**默认断点续提**：已有 h5 缓存且参数未变的病例跳过 |
| `run_radiomics_analysis` | `feature_csv`, `clinical`, `id_col`, `label_col`, `covariates`, `output_dir`（均可留空） | 先调用 `inspect_analysis_inputs()` 自动检测输入文件、ID 列、Label 列 → 若有歧义返回 `need_clarification` 由 LLM 向用户提问 → 确认后执行 LASSO + Logistic Regression 五折交叉验证 |
| `run_feature_statistics` | 同上 + `selected_features_csv` | 同理先 inspect 再确认执行 → 对选中特征做独立样本 t 检验 + Mann-Whitney U 检验 |

**分析结果的 JSON 安全转换**：

```python
# _json_safe_analysis_result: 剔除 oof 大数组（概率预测），只保留指标/特征/产物路径
# _json_safe_radiomics_result: 剔除 feature_df（pandas DataFrame），
#   特征名列表截断为 50 个，保留 n_features 总数
# _json_safe_stats_result: 剔除 results 大数组，只保留统计摘要计数
```

#### 5.1.5 元控制工具

| 工具 | 参数 | 确认 | 说明 |
|------|------|------|------|
| `update_todo_list` | `todos: List[Dict]` | **免确认** | 全量替换右侧步骤面板。每项含 `content` + `status`（pending/in_progress/completed），同时至多一个 in_progress |
| `ask_user_choice` | `question: str`, `options: List[str]` | **特殊交互** | 发起结构化提问。前端渲染为选择面板（选项按钮 + "其他"输入框）。options 限 2-8 个。不同于 interrupt——答案直接作为 ToolMessage 返回，图不挂起 |
| `dispatch_subagent` | `tasks: List[str]`, `mode: str = "general"` | explore 免确认；general 需确认 | 并行分派子任务。mode="explore" 时在 process_tool_calls 中直接执行不进入中断流程 |

**免确认执行的特殊工具**：

| 工具 | 说明 | 执行方式 |
|------|------|---------|
| `update_todo_list` | 全量替换 Todo List | 在 process_tool_calls 中直接写入 state，不进入中断 |
| `dispatch_subagent(explore)` | 只读探索子Agent | 在 process_tool_calls 中直接并行执行 |
| `ask_user_choice` | 结构化提问 | 设置 `pending_choice`，前端渲染选择面板，不走 human_review |
| `reformat_report` | 报告排版重排 | 在 process_tool_calls 中直接执行（幂等，原地保存） |
| `interpret_analysis_results` | LLM 结果解读 | 在 process_tool_calls 中直接执行（幂等，不叠加小节） |

#### 5.1.6 Word 文档工具

| 工具 | 参数 | 确认 | 说明 |
|------|------|------|------|
| `word_create` | `filename: str`, `content_markdown: str` | 需要 | 在项目目录下新建 Word 文档（.docx），套用中文学术论文格式（正文宋体小四、标题黑体、1.5 倍行距、表格五号）。`content_markdown` 用 markdown 组织内容（`#`/`##`/`###` 为各级标题，`-` 或 `*` 为列表项，`**粗体**` 保留加粗）。文件已存在时报错不覆盖 |
| `word_append` | `filename: str`, `content_markdown: str` | 需要 | 向已有 docx 追加 markdown 内容（格式同 `word_create`）。文件必须已存在 |
| `reformat_report` | 无 | **免确认** | 把最近一次分析输出的 `AutoRadiomics_Report.docx` 重排为中文学术格式。自动定位项目内最新的分析输出目录（含 `analysis_result.json`）。幂等，原地保存不生成备份。用户反馈报告格式混乱或要求"排版/重排报告"时调用 |

**Word 工具的学术格式规范**：由 `app/docx_style.py` 集中管理——正文中文宋体、西文/数字 Times New Roman、小四（12pt）、黑色、1.5 倍行距；Title 黑体三号（16pt）加粗居中；Heading 1 黑体四号（14pt）加粗黑色（显式覆盖 Word 默认蓝色主题）；Heading 2 黑体小四（12pt）加粗；表格五号（10.5pt）表头加粗居中；图片段落居中。中文字体通过 `w:eastAsia` XML 属性单独设置（python-docx 的 `font.name` 只设西文）。

#### 5.1.7 LLM 结果解读

| 工具 | 参数 | 确认 | 说明 |
|------|------|------|------|
| `interpret_analysis_results` | 无 | **免确认** | 对最近一次 `run_radiomics_analysis` 的分析结果生成 LLM 中文解读并注入报告。自动定位项目内最新的分析输出目录（含 `analysis_result.json`）。解读分三段：`【模型性能解读】`（AUC/敏感度/特异度/逐折稳定性）、`【特征意义解读】`（系数/OR/SHAP 方向一致性）、`【SHAP可解释性解读】`（top 特征贡献方向与幅度）。幂等——重复调用不会叠加小节。分析成功、报告生成后 Agent 应主动调用一次补全报告 |

### 5.2 工具注册的上下文裁剪

```python
# app/agent/tools.py - build_tools()
def build_tools(project_path, llm, allow_subagent=False, readonly=False):
    tools = {}

    # 只读工具：始终注册
    tools["list_directory"] = ...
    tools["find_files"] = ...
    tools["get_file_info"] = ...
    tools["read_yaml"] = ...
    tools["read_tabular_file"] = ...
    tools["discover_radiomics_pairs"] = ...
    tools["inspect_image_spacing"] = ...

    if not readonly:
        # 写/重操作工具：仅非只读模式注册
        tools["update_yaml"] = ...
        tools["plan_file_operations"] = ...
        tools["execute_python_script"] = ...
        tools["extract_radiomics_features"] = ...
        tools["run_radiomics_analysis"] = ...
        tools["run_feature_statistics"] = ...
        tools["update_todo_list"] = ...

    if allow_subagent and not readonly:
        # 子Agent 分派：仅主 Agent 可用，且嵌套深度 = 1
        tools["dispatch_subagent"] = ...

    # ask_user_choice 始终注册
    tools["ask_user_choice"] = ...
    return tools
```

三层开关的调用场景：

| 场景 | allow_subagent | readonly | 注册工具数 |
|------|:---:|:---:|:---:|
| 主 Agent（正常对话） | True | False | ~23 |
| 子 Agent（general模式） | False | False | ~22（无 dispatch_subagent） |
| 子 Agent（explore模式） | False | True | 8（仅只读探索工具） |

---

## 6. 人机协同机制

### 6.1 interrupt() 的完整生命周期

```
                    call_llm
                       │
                       ▼
                process_tool_calls
                       │
            ┌──────────┼──────────┐
            │ 免确认    │ 需确认    │
            ▼          ▼          │
       ToolMessage  设置 pending_* │
            │      interrupt_type  │
            │          │          │
            ▼          ▼          │
         call_llm   route_after_process
                        │
              ┌─────────┼─────────┐
              │ auto_approve?     │
              ├── Yes ──▶ auto_confirm → confirmed=True
              │
              └── No  ──▶ human_review
                              │
                         interrupt({
                           "type": interrupt_type,
                           "plan": pending_plan,
                           "command": pending_command,
                           "script": pending_script,
                           "radiomics_plan": pending_radiomics_plan,
                           "radiomics_execution": pending_radiomics_execution,
                           "radiomics_analysis": pending_radiomics_analysis,
                           "feature_statistics": pending_feature_statistics,
                           "subagent": pending_subagent,
                         })
                              │
                    图暂停 ⏸️
                              │
              ┌───────────────┼───────────────┐
              │               │               │
         POST /confirm   POST /cancel   POST /other
         resume={         resume={        resume={
           action:          action:         action:
           "confirm"}       "cancel"}       "other",
                                           instruction}
              │               │               │
              ▼               ▼               ▼
         confirmed=True  confirmed=False confirmed=False
                                        other_instruction=...
              │               │               │
              └───────────────┴───────────────┘
                              │
                              ▼
                     execute_confirmed
                              │
                  ┌───────────┴───────────┐
                  │ confirmed?            │
                  ├── True ──▶ 实际执行    │
                  │           结果注入     │
                  │           executed=True│
                  │                       │
                  └── False ─▶ cancelled   │
                               ToolMessage │
                               + 可选       │
                               HumanMessage│
                              │
                              ▼
                     _clear_interrupt()
                     （清空所有 pending_*）
                              │
                              ▼
                         call_llm
```

### 6.2 前端审批面板与 interrupt_type 的映射

| interrupt_type | 前端面板组件 | 用户可做的操作 |
|---------------|-------------|---------------|
| `system_command` | `CommandPanel.vue` | 查看命令详情 → 确认 / 取消 |
| `file_plan` | `PlanEditor.vue` | 查看/编辑文件操作计划（增删改条目）→ 确认 / 取消 |
| `python_script` | `ScriptPanel.vue` | 查看代码（高亮 + 风险标记）→ 确认 / 取消 |
| `radiomics_plan` | `RadiomicsPanel.vue` | 查看配对→ 确认（进入提取参数）/ 取消 |
| `radiomics_execution` | `RadiomicsPanel.vue` | 查看提取参数（YAML路径/输出目录/病例数）→ 确认 / 取消 |
| `radiomics_analysis` | `RadiomicsPanel.vue` | 查看分析参数（特征CSV/临床表/ID列/Label列）→ 确认 / 取消 |
| `feature_statistics` | `RadiomicsPanel.vue` | 查看统计参数 → 确认 / 取消 |
| `subagent_dispatch` | `ApprovalPanel.vue` | 查看子任务列表 → 确认 / 取消 |

**ask_user_choice 的特殊处理**：不经过 `human_review` 中断流程。`process_tool_calls` 中直接设置 `pending_choice` + `interrupt_type = "choice"`，前端渲染 `ChoicePanel`，用户选择后答案通过专用 API 写入 `choice_answer` 并作为 ToolMessage 返回给 LLM。

### 6.3 取消的语义处理

用户取消时，`execute_confirmed` 的行为因操作类型而异：

```
if 简单查询取消 (system_command):
    → 仅返回 cancelled ToolMessage
    → LLM 自然知道"操作没做"，不会误判

if 重操作取消 (file_plan / python_script / radiomics_* /
              feature_statistics / subagent_dispatch):
    → 返回 cancelled ToolMessage
    → 追加 HumanMessage("我取消了刚才的操作，请不要重试。请询问我现在想做什么。")
    → 防止 LLM 将"取消结果"误读为"操作失败"而自动重试
```

### 6.4 执行结果的防重问标记

所有已确认执行的操作结果注入：

```json
{
  "executed": true,
  "note": "用户已确认，操作已执行完成。请直接向用户总结执行结果，不要再要求确认。",
  ...原有字段...
}
```

System Prompt（`agent-core` SKILL.md）中也明确告知 LLM：`"Tool results wrapped as {"executed": true, ...} report operations that have ALREADY run after user confirmation. Summarize the outcome to the user; never ask for confirmation again."`

---

## 7. 子Agent系统

### 7.1 设计动机与核心思想

完整的影像组学分析需要在项目目录中探索大量信息：目录结构、文件清单、YAML 配置、临床表格列名、图像/mask 配对情况。如果串行在主对话中完成，每一步的中间结果都累积在消息历史中，迅速消耗上下文窗口。

子Agent系统的核心思想：
1. **并行加速**：多个独立探索任务同时运行
2. **上下文隔离**：子Agent 的中间过程不进主对话，只有结论返回
3. **安全分级**：explore 模式只读，general 模式需确认

### 7.2 两种模式的完整对比

| 维度 | `mode="explore"` | `mode="general"` |
|------|-----------------|-----------------|
| **工具集** | 9 个（只读探索 + ask_user_choice） | 15 个（全部除 dispatch_subagent） |
| **confirm 流程** | 无——在 process_tool_calls 中直接执行 | 有——走 auto_confirm（子Agent 内部 auto_approve=True） |
| **可见性** | 中间过程只推前端，不进主对话 | 中间过程只推前端，不进主对话 |
| **典型用法** | 项目开局探索：扫目录/读配置/查临床表 | 需要写操作或计算的子任务 |
| **并行上限** | 4 worker | 4 worker |
| **LLM 调用** | 每次工具调用自动批准 | 每次工具调用自动批准（但 high 脚本危险标记仍保留） |

### 7.3 隔离机制的实现细节

```python
# _run_subagent 的核心流程

def _run_subagent(task, state, config, parent_thread_id, mode):
    # 1. 派生 thread_id
    sub_thread_id = f"{parent_thread_id}:sub:{uuid4().hex[:8]}"

    # 2. 注册运行时上下文（共享父线程取消事件）
    parent_ctx = agent_runtime.get(parent_thread_id)
    sub_ctx = agent_runtime.register(sub_thread_id)
    if parent_ctx is not None:
        sub_ctx.cancel_event = parent_ctx.cancel_event  # 取消传播

    # 3. 构建独立图 + 独立 MemorySaver
    sub_graph = create_agent_graph()  # 默认 MemorySaver，不共享父 checkpoint

    # 4. 组装子配置
    sub_config = {
        "configurable": {
            "thread_id": sub_thread_id,
            "auto_approve": True,        # 免确认，不会 interrupt
            "allow_subagent": False,     # 禁止再嵌套分派
            "readonly_tools": mode == "explore",
            "api_key": ...,
        },
        "recursion_limit": 150,          # ~37 轮工具调用
    }

    # 5. 注入专用 System Prompt
    system_prompt = (
        EXPLORE_SUBAGENT_SYSTEM_PROMPT  # "你是只读探索子 agent..."
        if mode == "explore"
        else SUBAGENT_SYSTEM_PROMPT      # "你是全功能子 agent..."
    )

    # 6. 流式运行
    sub_input = {
        "messages": [SystemMessage(content=system_prompt),
                     HumanMessage(content=task)],
        "project_path": state["project_path"],
        ...
    }

    for values in sub_graph.stream(sub_input, sub_config, stream_mode="values"):
        # 滚动推送中间过程到前端
        status["entries"] = _summarize_subagent_entries(values["messages"])
        _publish_subagent(parent_thread_id, status, persist=False)

        # 检查取消
        if sub_ctx.cancel_event.is_set():
            cancelled = True
            break

    # 7. 提取结论
    conclusion, usage = _extract_subagent_result(final["messages"])
    # 结论 = 最后一条非空 AIMessage.content
    # usage = 所有消息 usage_metadata 的累加

    # 8. 截断返回
    return {
        "success": True,
        "result": conclusion[:_SUBAGENT_RESULT_MAX_CHARS],  # 4000 字符
        "usage": usage,
    }
```

### 7.4 上下文截断策略

```
子Agent 消息历史:
  [SystemMessage, HumanMessage(task),
   AIMessage(tool_calls), ToolMessage, AIMessage(content),
   AIMessage(tool_calls), ToolMessage, AIMessage(content),
   ...中间过程...,
   AIMessage(最终结论)]

进入主对话的内容:
  → 最终结论截断为 4000 字符 (_SUBAGENT_RESULT_MAX_CHARS)
  → 累计 token 用量汇总到 dispatch_subagent 返回的 usage 字段

推送到前端的内容:
  → 滚动窗口：最近 8 条消息摘要 (_SUBAGENT_ENTRY_WINDOW)
  → 每条摘要截断为 300/200 字符
  → 中间滚动 persist=False（不写 SQLite），开始/结束 persist=True
```

### 7.5 并行执行

```python
# _run_subagents 中的并发控制

if len(tasks) == 1:
    results = [_run_subagent(tasks[0], ...)]          # 单任务：内联执行
else:
    workers = min(len(tasks), _SUBAGENT_MAX_WORKERS)  # 最多 4 并发
    with ThreadPoolExecutor(max_workers=workers) as pool:
        futures = [pool.submit(_run_subagent, task, ...) for task in tasks]
        results = [f.result() for f in futures]        # 阻塞等待全部完成
```

每个子Agent 在独立线程中运行，各自持有独立的图实例和 MemorySaver。线程安全由各模块的独立实例保证（无共享状态）。

### 7.6 取消传播链

```
用户点击 /stop
  → request_cancel(parent_thread_id)
  → parent_ctx.cancel_event.set()

子Agent 中检测：
  → 每次 stream_mode="values" 迭代后检查 sub_ctx.cancel_event.is_set()
  → 耗时操作（FeatureAgent.run）在每个病例完成后检查 cancel_event
  → 检测到取消 → 立即退出 → status="cancelled" → 返回 {"success": False, "cancelled": True}
```

---

## 8. 流式通信协议

### 8.1 为什么绕过 LangChain 的 ChatOpenAI

LangChain 的 `ChatOpenAI` 在流式模式下调用 `openai.ChatCompletion.create(stream=True)`，内部用 `ChatCompletionChunk` 拼接最终 `AIMessage`。DeepSeek 在标准 OpenAI 协议之外增加了 `reasoning_content` 字段（思考链），该字段出现在 delta chunk 中但**不在**最终的 `ChatCompletion` 响应里。LangChain 的拼接逻辑不认识这个字段，直接丢弃。

解决方案：在 `call_llm` 中直接用**原生 OpenAI SDK** 做流式调用：

```python
# _stream_chat_completion 的关键逻辑

stream = client.chat.completions.create(
    model=model,
    messages=convert_to_openai_messages(messages),
    tools=[convert_to_openai_tool(t) for t in tools],
    temperature=0.2,
    parallel_tool_calls=False,
    stream=True,
    stream_options={"include_usage": True},
)

reasoning_parts = []       # 思考链累积
content_parts = []          # 文本累积
tool_slots = {}             # 按 index 分槽累积 tool_calls

for chunk in stream:
    # usage chunk（开启 include_usage 后流末尾会有一个仅含 usage 的 chunk）
    if chunk.usage:
        usage_metadata = {...}

    delta = chunk.choices[0].delta

    # reasoning_content：非标准字段，每 chunk 带增量或全量
    if delta.reasoning_content:
        reasoning_parts.append(delta.reasoning_content)
        _publish_thinking(thread_id, "".join(reasoning_parts), done=False)

    # content：标准字段
    if delta.content:
        content_parts.append(delta.content)

    # tool_calls：按 index 分槽累积（OpenAI 流式中同一个 tool_call 跨多个 chunk 发送）
    for tc in (delta.tool_calls or []):
        slot = tool_slots.setdefault(tc.index, {"id": "", "name": "", "arguments": ""})
        if tc.id: slot["id"] += tc.id
        if tc.function:
            if tc.function.name: slot["name"] += tc.function.name
            if tc.function.arguments: slot["arguments"] += tc.function.arguments

# 流结束
_publish_thinking(thread_id, full_reasoning, done=True)

# 组装 AIMessage
return AIMessage(
    content="".join(content_parts),
    tool_calls=[...],  # 按 index 排序，arguments JSON.parse
    additional_kwargs={"reasoning_content": full_reasoning},  # 随 checkpoint 持久化
    usage_metadata=usage_metadata,
)
```

### 8.2 SSE 事件体系

#### 8.2.1 事件类型一览

| 事件来源 | SSE event 类型 | persist | 触发时机 | 数据格式 |
|---------|---------------|---------|---------|---------|
| graph values 快照 | `agent` | ✅ | 每个 superstep 结束 | `AgentState`（messages + interrupt_type + pending_* + todos + context_usage + running） |
| 思考链 | `agent` | ❌ | 流式 chunk 到达 | `{"thinking": {"text": "...", "done": false}, "running": true}` |
| 提取进度 | `agent` | ✅ | 每个病例提完 | `{"radiomics_progress": {"stage": "extracting", "current": 5, "total": 30, "patient_id": "..."}, "running": true}` |
| 子Agent 状态 | `agent` | 开始/结束 ✅，中间滚动 ❌ | 子Agent 状态变化 | `{"subagent": {"id": "...", "task": "...", "status": "running", "entries": [...]}, "running": true}` |
| 流结束 | `agent_end` | — | async generator 正常结束 | `{}` |
| 心跳 | `: keep-alive` | — | 每秒（无新事件时） | 注释行 |

#### 8.2.2 persist 策略的设计逻辑

```
高频事件 (persist=False) — 每秒可能推送多次:
  - thinking delta: 推理过程中每个 token chunk 都推送累积全文
  - subagent 中间滚动: 每个 tool call 轮次都更新

原因:
  - SQLite 写入成为瓶颈（每秒数十次 INSERT）
  - 回放无价值（用户只需要看到最新状态）

兜底:
  - 页面刷新后调用 GET /threads/{id} 获取完整 values 快照
  - 快照中 AIMessage.additional_kwargs.reasoning_content 含完整思考链
  - 子Agent 开始/结束状态 persist=True（重连后可判断是否有正在运行的子Agent）

低频事件 (persist=True):
  - graph values 快照: 每个 superstep 一次
  - 提取进度: 每个病例一次（典型场景 30-100 个病例）
  - 子Agent 开始/结束
```

#### 8.2.3 进度补偿机制

```
场景：用户刷新页面时，特征提取正在进行中

正常流程（无刷新）:
  extract case 1 → publish(radiomics_progress, persist=True)
  extract case 2 → publish(radiomics_progress, persist=True)
  ...

刷新后:
  GET /threads/{id} → values 快照（不含 radiomics_progress，因为不在 state 中）
  连接 SSE → 只接收新事件（订阅时的 last_event_id = 最新持久化事件 id）

问题：刷新前的进度事件不会重放，前端进度条消失。

解决方案（在 thread_events 端点中）:
  if fresh_subscribe and thread_id in active_agent_streams:
      latest = bridge.store.get_latest_sse_event_containing(
          "agent", thread_id, "radiomics_progress"
      )
      if latest:
          catch_up_data = latest["data"]  # 最近一条含 radiomics_progress 的持久化事件

  在 event_generator 中:
      if catch_up_data:
          yield f"event: agent\ndata: {catch_up_data}\n\n"  # 不带 id 行！
          # ↑ 不带 id 是为了让浏览器的 Last-Event-ID 保持未设置，
          # 自动重连时走同样的补偿逻辑而非回放全部历史
```

### 8.3 EventBridge 设计

```python
# app/api/sse.py - EventBridge 的核心数据结构

class EventBridge:
    store: ProjectStore          # SQLite 持久化
    _queues: Dict[str, Dict[int, asyncio.Queue]]
    # "agent:{thread_id}" → {id(queue): asyncio.Queue(maxsize=256)}
    _locks: Dict[str, asyncio.Lock]    # 每 scope 一把锁
    _next_ids: Dict[str, int]           # 每 scope 的单调 id 分配器
    _pending_writes: Dict[str, Set[asyncio.Task]]  # 每 scope 的进行中写任务
```

**publish 的原子性保证**：

```python
async def publish(scope, scope_id, data, *, persist=True):
    async with lock:                          # scope 锁
        event_id = allocate_event_id()        # 单调递增分配

        if persist:
            write_task = asyncio.ensure_future(
                run_in_threadpool(store.record_sse_event, ...)
            )
            track_write(write_task)
            await asyncio.shield(write_task)  # 写任务不被发布者取消传播

        for queue in subscribers.values():    # 推送到在线订阅者
            if queue.full():
                queue.get_nowait()            # 丢弃最旧事件
            await queue.put({"event_id": event_id, "data": data})

    return event_id
```

**subscribe 的间隙消除**：

```python
async def subscribe(scope, scope_id, last_event_id):
    async with lock:
        await await_pending_writes()          # 等所有进行中写任务落库
        max_event_id = next_event_id() - 1    # 确认数据库最大 id

        register_queue()                      # 注册新订阅者
        historical = store.list_sse_events(   # 回放历史事件
            after_event_id=last_event_id)
        for event in historical:
            if event["event_id"] <= max_event_id:
                await queue.put(event)        # 先放历史

    return queue                              # 之后放实时的
    # 在锁内完成回放+注册，保证不遗漏间隙事件
```

### 8.4 前端 SSE 消费

```typescript
// frontend/src/api/agent.ts - connectAgentEvents

const es = new EventSource(`${API_BASE}/agent/threads/${threadId}/events`)

es.addEventListener('agent', (event: MessageEvent) => {
    const data = JSON.parse(event.data) as AgentState
    callbacks.onState?.(data)  // → applyState() → 更新 Pinia store
})

es.addEventListener('agent_end', () => {
    callbacks.onEnd?.()
    // → busy = false, radiomicsProgress = null, currentThinking = null
    // → syncThread() 获取最终 state 保证收敛
    // → listThreads() 刷新侧边栏（首轮对话后标题已生成）
})

es.onerror = () => {
    callbacks.onError?.({ message: 'EventSource error' })
    // → currentThinking = null (陈旧 thinking 无快照兜底)
    // → disconnect()
    // 浏览器会自动重连（EventSource 标准行为），重连时携带 Last-Event-ID
}
```

**前端状态更新的关键约束**：`applyState()` 中 `busy` 完全由后端上报的 `running` 字段驱动，**不能**由 `interrupt_type` 推断。原因：`execute_confirmed` 清除 `interrupt_type` 前会有一个中间快照仍带旧值——如果用 `interrupt_type != null` 判断 busy，确认后会短暂解除 busy 再恢复，导致 UI 闪烁。

---

## 9. 安全设计

### 9.1 路径沙箱

```python
# app/agent/safety.py

class Sandbox:
    def __init__(self, root: str):
        self.root = Path(root).resolve()      # 必须是已存在目录

    def resolve(self, path, must_exist=False):
        target = (self.root / path).resolve()  # 相对路径从 root 起算
        target.relative_to(self.root)          # 越界抛 ValueError
        if must_exist and not target.exists():
            raise FileNotFoundError(...)
        return target
```

所有工具函数在访问文件系统前**必须**通过 `sandbox.resolve()`。`execute_confirmed` 中有专门的 `PathEscapeError` 捕获（转义错误 → 返回 `{"success": False, "error": "路径超出项目目录"}`）。

### 9.2 确认-执行分离

```
传统 Agent 工具设计:
  LLM 调用 → 工具函数直接执行 → 返回结果

OneRad 工具设计:
  LLM 调用 → 工具函数校验参数 → 返回 {"_pending_tool": ..., 参数}
           → process_tool_calls 识别为需确认
           → human_review / auto_confirm
           → execute_confirmed 实际执行
```

这种两层设计的优势：
1. **LLM 和用户在审批面板看到相同的参数**——由工具函数校验后生成，而非 LLM 直接拼 JSON
2. **用户可在审批前编辑**——如修改文件操作计划的路径、增删条目
3. **审批面板可以格式化展示**——代码高亮、配对置信度表格、参数列表

### 9.3 纵深防御链

```
第一层: System Prompt (agent-core SKILL.md)
  "Respect every confirmation step, sandbox boundary, schema constraint,
   and risk decision enforced by the application."

第二层: 工具函数参数校验
  - extract_radiomics_features: 校验 YAML 存在 + 路径在沙箱内 + YAML 可解析
  - plan_file_operations: validate_plan() 限制四种操作 + 沙箱检查
  - execute_python_script: classify_risk() AST 静态扫描

第三层: process_tool_calls 确认拦截
  - 所有副作用工具必须通过 human_review/auto_confirm

第四层: execute_confirmed 运行时沙箱
  - Sandbox.resolve() 路径解析
  - 中危脚本注入沙箱头（运行时 hook open()）

第五层: 操作系统
  - Docker 容器 + 只读挂载（生产部署时）
```

---

## 10. Skills 系统

### 10.1 设计理念

System prompt 不是硬编码在 Python 中的字符串，而是放在 `skills/<name>/SKILL.md` 下的 Markdown 文件。每次 LLM 调用时从磁盘实时读取，**无任何缓存**。

好处：
- 调整 Agent 行为只需编辑 Markdown 文件，无需重启服务
- 非编程人员也可以修改 prompt（调整工作流步骤、措辞风格等）
- Git 版本控制友好，prompt 变更可追溯

### 10.2 加载机制

```python
# app/skills.py

SKILLS_DIR = Path(__file__).resolve().parent.parent / "skills"
_SKILL_NAME_PATTERN = re.compile(r"^[a-z0-9]+(?:-[a-z0-9]+)*$")

def load_skill(name: str) -> str:
    path = SKILLS_DIR / name / "SKILL.md"
    markdown = path.read_text(encoding="utf-8")
    body = _markdown_body(markdown)  # 剥离 YAML frontmatter（`---\n...\n---`）
    if not body: raise SkillLoadError(...)
    return body

def load_skill_bundle(names: Iterable[str]) -> str:
    sections = []
    for name in names:
        sections.append(f"<!-- OneRad skill: {name} -->\n{load_skill(name)}")
    return "\n\n---\n\n".join(sections)
```

主 Agent 调用：`load_skill_bundle(("agent-core", "radiomics-workflow"))`

### 10.3 当前 Skill 清单

| Skill | 文件路径 | 大小 | 核心职责 |
|-------|---------|------|---------|
| `agent-core` | `skills/agent-core/SKILL.md` | ~1KB | Agent 角色定义：工作行为准则（先探索再修改、工具结果为准、executed 标记的含义、安全边界不可绕过） |
| `radiomics-workflow` | `skills/radiomics-workflow/SKILL.md` | ~2KB | 影像组学工作流 0-6 步骤：先 explore 探索 → 配对发现 → spacing 检查 → 参数确认 → 特征提取 → 临床表审查 → 建模 → 报告。强调每进入/完成阶段调用 update_todo_list |
| `file-operations` | `skills/file-operations/SKILL.md` | — | `plan_file_operations` 工具的 prompt 模板：如何从用户需求生成 move/copy/rename/mkdir 计划 |
| `clinical-columns` | `skills/clinical-columns/SKILL.md` | — | 帮助 LLM 从临床表列名中识别患者 ID、二分类 Label、协变量 |
| `filename-id` | `skills/filename-id/SKILL.md` | — | 从 NIfTI 文件名中推断患者 ID 的正则模式 |
| `report-writing` | `skills/report-writing/SKILL.md` | — | Word 报告的方法学描述润色提示 |
| `thread-title` | `skills/thread-title/SKILL.md` | — | 从用户首条消息生成 15 字以内的对话标题 |
| `word-report` | `skills/word-report/SKILL.md` | — | Word 文档的中文学术论文格式规范（宋体小四/黑体标题/1.5倍行距/表格五号），以及 `word_create`/`word_append`/`reformat_report` 三个工具的使用指引 |
| `result-interpretation` | `skills/result-interpretation/SKILL.md` | — | 影像组学分析结果的三段式中文解读 prompt：基于数值摘要（模型指标、逐折稳定性、特征系数、SHAP 重要性）撰写 `【模型性能解读】`/`【特征意义解读】`/`【SHAP可解释性解读】`，三段末尾附局限性声明。严格禁止编造数据或引用未提供的信息 |

### 10.4 agent-core SKILL.md 的关键指令

```
- 收到"开始分析"类请求时，优先用 dispatch_subagent(mode="explore") 并行探索项目
- 工具结果是唯一真相源。不要声称文件/分析存在，除非工具结果确认了它。
- 被 {"executed": true, ...} 包裹的结果表示操作已执行。总结给用户，不要再问确认。
- 遇到需要向用户澄清的问题时，使用 ask_user_choice 让用户在几个明确方案中选择。
- 尊重每一个确认步骤、沙箱边界、schema 约束和风险决策。不要绕过被拒绝的操作。
```

---

## 11. Agent 工作流程与增量分析设计

### 11.1 设计背景

影像组学分析是一个多步骤的流程，每一步的产出是下一步的输入。在实际使用中，用户很少一次性跑通全流程——更常见的场景是：

- 上次提取了特征但没做分析，这次只想从分析开始
- 已有特征 CSV，想换一组参数重新建模
- 新增了几个病例的图像，需要增量提取（已有病例跳过）
- 不确定某个中间文件是否还能用，希望 Agent 自己判断

因此，Agent 被设计为**具备项目状态感知能力**——它能够在执行前扫描项目已有文件，识别可复用的中间产物，推断当前流程的真实起点，并在此基础上增量推进。

### 11.2 完整分析工作流

以下是 Agent 在 radiomics-workflow SKILL.md 引导下执行的 0–6 步标准工作流：

```
用户: "开始分析"
        │
        ▼
┌─ 步骤 0: 项目探索 (dispatch_subagent, mode="explore") ─────────────────┐
│                                                                          │
│  并行派发 2-4 个只读子Agent:                                              │
│    Task 1: 扫描目录结构，列出所有 .nii.gz / .csv / .xlsx / .yaml         │
│    Task 2: 检查 images/ 和 masks/，评估配对情况                          │
│    Task 3: 读取 Params_labels.yaml，总结提取参数设置                     │
│    Task 4: 搜索临床表格，列出列名和样本数                                 │
│                                                                          │
│  子Agent 并行运行 → 结论汇总 → 主Agent 获得项目全貌                        │
│                                                                          │
│  关键判断: 是否有可复用的已有产出？                                        │
│    - radiomics_features/radiomics_features.csv 存在?                    │
│    - radiomics_features/h5/*.h5 存在?                                   │
│    - radiomics_analysis/ 目录存在?                                       │
│    - feature_statistics/ 目录存在?                                       │
│                                                                          │
│  → 有可复用产出: 调用 update_todo_list()，将已完成步骤标记 completed,       │
│                 从实际断点开始 in_progress                                 │
│  → 无可复用产出: 调用 update_todo_list()，建立完整 6 步清单               │
└──────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─ 步骤 1: 配对发现 (discover_radiomics_pairs) ────────────────────────────┐
│                                                                          │
│  工具: discover_radiomics_pairs()                                        │
│  后端: app/radiomics_discovery.py → 递归扫描 images/ + masks/            │
│        → 三级置信度配对 (high/medium/low) + 未匹配列表                     │
│                                                                          │
│  确认后面板展示配对结果，用户可:                                           │
│    - 修改配对（调整 image↔mask 对应关系）                                  │
│    - 移除低置信度配对                                                     │
│    - 添加手动配对                                                        │
│                                                                          │
│  已有产出检测: 若 images/ 目录下已有 .nii.gz，且之前配对过，                │
│               discover 的结果应与上次一致；如有新增病例，重新扫描           │
└──────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─ 步骤 2: 参数确认 (spacing 检查 + YAML 配置) ────────────────────────────┐
│                                                                          │
│  工具: inspect_image_spacing(pairs) + read_yaml + update_yaml            │
│                                                                          │
│  流程:                                                                    │
│    1. 对已确认的 pairs 做 spacing 检测                                    │
│       → 输出各轴 spacing 的中位数/范围/不同取值数                          │
│       → 若 ≤50 例: 附逐例明细                                             │
│       → 给出 resampledPixelSpacing 建议值                                 │
│                                                                          │
│    2. 比较当前 YAML 中的 resampledPixelSpacing                           │
│       → 一致: 确认参数，进入提取                                          │
│       → 不一致: 用 ask_user_choice 询问用户是否调整                        │
│         "当前 YAML spacing=[3,3,3]，实测中位数=[1,1,5]，建议改为[1,1,5]"  │
│                                                                          │
│    3. 若用户要调整 → update_yaml() 修改                                   │
│       → 注意: YAML 修改会使旧的 h5 缓存失效（参数 hash 变化），            │
│         下次提取时这些病例自动重提                                         │
│                                                                          │
│  为什么 spacing 检查在配对之后而非之前？                                    │
│    - spacing 依赖于已确认的配对列表（只检测用户认可的病例）                  │
│    - 先确定"要分析哪些病例"，再逐个检查它们的 spacing                       │
└──────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─ 步骤 3: 特征提取 (extract_radiomics_features) ──────────────────────────┐
│                                                                          │
│  工具: extract_radiomics_features(pairs, yaml_path, force_rerun)         │
│  后端: app/feature.py → FeatureAgent.run()                               │
│                                                                          │
│  核心机制: 断点续提 (Resume)                                               │
│    - 每次提取一个病例后，PyRadiomics 将特征向量写入 h5/<patient_id>.h5    │
│    - 下次提取前，检查:                                                     │
│        1. h5 文件是否存在?                                                │
│        2. h5 中的提取参数 hash == 当前 YAML 参数 hash?                    │
│      → 都满足: 跳过该病例 (resumed)                                       │
│      → 不满足: 重新提取                                                    │
│    - force_rerun=True: 忽略上述检查，全部重提                              │
│                                                                          │
│  进度推送:                                                                 │
│    FeatureAgent 每完成一个病例 → progress_callback({                       │
│      stage: "extracting",                                                 │
│      current: 5, total: 30,                                               │
│      patient_id: "P001"                                                   │
│    }) → _publish_agent_progress → SSE → 前端进度条                        │
│                                                                          │
│  取消支持:                                                                 │
│    FeatureAgent 在每个病例完成后检查 cancel_event.is_set()                 │
│    → 已提取的 h5 文件保留，下次可从此处续提                                 │
│    → 保证 /stop 不会浪费已完成的工作                                       │
│                                                                          │
│  错误容忍:                                                                 │
│    单个病例提取失败 → 记录到 failed_cases.csv，继续下一个                   │
│    → 不会因一个坏数据打断整批提取                                          │
│                                                                          │
│  已有产出检测:                                                             │
│    若 force_rerun=False 且 h5 缓存覆盖全部病例:                             │
│    → "所有 N 个病例均已完成提取，无需重复。是否强制重提？"                   │
│    若部分覆盖:                                                             │
│    → 仅提取剩余病例，已完成的标记为 skipped                                 │
│                                                                          │
│  产物:                                                                     │
│    radiomics_features/radiomics_features.csv  (合并后的特征矩阵)           │
│    radiomics_features/failed_cases.csv         (失败记录)                  │
│    radiomics_features/h5/<patient_id>.h5       (每例缓存)                  │
└──────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─ 步骤 4: 临床表审查 ─────────────────────────────────────────────────────┐
│                                                                          │
│  工具: read_tabular_file + read_yaml (project.yaml)                       │
│  Skill: clinical-columns                                                 │
│                                                                          │
│  流程:                                                                    │
│    1. 自动搜索项目内临床 CSV/Excel                                        │
│    2. 读取列名 → LLM 识别 ID 列、Label 列、可用协变量                      │
│    3. 若有歧义（多列候选）→ need_clarification → LLM 向用户提问            │
│    4. 确认后进入分析                                                      │
│                                                                          │
│  注意: 提取特征不需要临床表，但建模需要。所以这一步在提取之后。              │
└──────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─ 步骤 5: 建模分析 (run_radiomics_analysis) ───────────────────────────────┐
│                                                                          │
│  工具: run_radiomics_analysis(feature_csv, clinical, id_col,              │
│         label_col, covariates, output_dir, n_splits,                      │
│         max_lasso_features, random_state)                                  │
│  后端: app/radiomics_analysis.py → LASSO + LogisticRegression 五折CV     │
│        + app/analysis.py → 逐折 SHAP 可解释性分析                         │
│                                                                          │
│  分析前参数询问:                                                           │
│    调用 run_radiomics_analysis 前，Agent 必须先用 ask_user_choice         │
│    询问用户是否调整分析参数：                                               │
│    - n_splits（交叉验证折数，默认 5）                                      │
│    - max_lasso_features（LASSO 最大特征数，默认 100）                      │
│    - random_state（随机种子，默认 42）                                     │
│    - covariates（协变量列表）                                              │
│    用户要调整时通过对应参数传入，不调整时留空使用默认值                      │
│                                                                          │
│  参数自动推断:                                                             │
│    - feature_csv 缺省 → radiomics_features/radiomics_features.csv       │
│    - clinical 缺省 → 自动搜索项目内 CSV/Excel                             │
│    - id_col / label_col 缺省 → 自动识别                                   │
│    - 若有歧义 → 返回 need_clarification → LLM 向用户确认                  │
│                                                                          │
│  临床列名中译英:                                                           │
│    合并数据后自动检测非 ASCII 临床列名 → LLM 翻译为英文                     │
│    → 避免 matplotlib SHAP 图中文乱码及报告字体问题                         │
│    → column_name_mapping 写入 analysis_params.json 保证复现确定性         │
│                                                                          │
│  SHAP 可解释性（逐折）:                                                    │
│    每折训练完成后自动执行 SHAP 分析：                                       │
│    - LinearExplainer 优先（线性模型），KernelExplainer 回退                 │
│    - 每折落盘: shap_summary_fold{N}.png / shap_bar_fold{N}.png /          │
│      shap_values_fold{N}.csv                                              │
│    - 报告嵌入：fold-1 LASSO path + 全部逐折 SHAP 图                        │
│                                                                          │
│  复现保证:                                                                 │
│    输出目录保存 analysis_params.json（参数快照）+ run_analysis.py          │
│    （完整复跑脚本），用户可独立执行复现分析                                  │
│                                                                          │
│  已有产出检测:                                                             │
│    若 radiomics_analysis/ 下已有结果:                                     │
│    → Agent 会报告已有分析结果                                              │
│    → 询问用户: 覆盖重跑 / 仅查看已有结果 / 用新参数重跑?                   │
│                                                                          │
│  产物:                                                                     │
│    radiomics_analysis/selected_features.csv   (LASSO 筛选后的特征)         │
│    radiomics_analysis/metrics.json            (AUC, 准确率, 敏感性等)      │
│    radiomics_analysis/analysis_params.json    (参数快照，供复现)           │
│    radiomics_analysis/run_analysis.py         (复跑脚本)                   │
│    radiomics_analysis/report.md               (Markdown 报告 + 解读)       │
│    radiomics_analysis/report.docx             (完整 Word 报告 + 解读)      │
│    radiomics_analysis/interpretation.md       (解读纯文本)                 │
│    radiomics_analysis/figures/                 (ROC/校准/DCA 曲线图)        │
│    radiomics_analysis/lasso/                   (逐折 LASSO path 图)        │
│    radiomics_analysis/shap/                    (逐折 SHAP 图 + CSV)        │
│    radiomics_analysis/predictions/             (逐折预测概率)              │
│    radiomics_analysis/curves/                  (逐折 ROC/校准/DCA)         │
└──────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─ 步骤 6: 结果解读与报告 ──────────────────────────────────────────────────┐
│                                                                          │
│  Agent 读取分析结果 (metrics.json + report.md) → 向用户做结构化总结:       │
│    - 模型性能 (AUC, 准确率, 敏感性, 特异性)                                │
│    - 关键特征 (LASSO 筛选出的影像组学特征)                                 │
│    - 校准与临床获益 (校准曲线 + DCA)                                       │
│    - 产物路径 (报告、图片、特征表格)                                       │
│    - 局限性与后续建议                                                      │
│                                                                          │
│  自动补全: Agent 在分析成功后主动调用 interpret_analysis_results           │
│    → LLM 读取 metrics.json + SHAP CSV → 生成三段中文解读                  │
│    → 注入 report.md（追加"结果解读"小节）和 report.docx（同步更新）         │
│    → interpretation.md 落地（纯文本存档）                                  │
│    → 幂等——重复调用不会叠加小节                                            │
│                                                                          │
│  可选: run_feature_statistics → 对选中特征做 t 检验 + MWU                  │
│    产物: feature_statistics/统计表格.docx（docx_style 学术格式）           │
│                                                                          │
│  可选: reformat_report → 把分析报告重排为中文学术格式（宋体小四/黑体标题）   │
│    免确认、幂等、原地保存                                                  │
└──────────────────────────────────────────────────────────────────────────┘
```

### 11.3 项目状态感知：已有文件识别

Agent 不是"瞎子"——它在每次操作前都会检查项目目录中已有的文件，据此决定行为。这套机制分布在多个层面：

#### 11.3.1 探索阶段的全景扫描

```
用户说"开始分析" → Agent 的第一步永远是 dispatch_subagent(mode="explore")

explore 子Agent 的典型扫描逻辑:
  1. list_directory(".") → 了解顶层结构
  2. find_files("*.nii.gz", "images") → 统计图像文件
  3. find_files("*.csv", "radiomics_features") → 检查是否已有特征 CSV
  4. find_files("*.csv", "radiomics_analysis") → 检查是否已有分析结果
  5. read_yaml("Params_labels.yaml") → 读取提取参数
  6. glob 搜索临床表格: find_files("*.csv") + find_files("*.xlsx")

汇总判断:
  - 完全没有中间产物 → 全新分析，从步骤 1 开始
  - 有配对 + YAML，无 features → 从步骤 3 (提取) 开始
  - 有 features CSV，无 analysis → 从步骤 5 (分析) 开始
  - 全部都有 → 询问用户：查看报告？换参数重跑？新增病例？
```

#### 11.3.2 提取阶段的缓存感知

```python
# FeatureAgent.run() 的核心逻辑（简化）

for pair in pairs:
    h5_path = f"h5/{patient_id}.h5"
    if not force_rerun and h5_path.exists():
        cached_params_hash = read_hash_from_h5(h5_path)
        current_params_hash = hash(yaml_content)
        if cached_params_hash == current_params_hash:
            skip_count += 1
            continue          # ← 跳过，不重复提取
    try:
        extract_one(pair)
        success_count += 1
    except Exception:
        failed_count += 1

return {"n_success": success_count, "n_skipped": skip_count, ...}
```

这是一种**基于内容哈希的缓存失效策略**：不依赖时间戳，而是检查提取参数是否真正发生了变化。用户改了 YAML 中的 `resampledPixelSpacing` 后，受影响的病例自动重提；没改参数则直接复用。

#### 11.3.3 分析阶段的输入自动推断

```python
# run_radiomics_analysis 的参数推断逻辑

if not feature_csv:
    # 先找默认路径
    candidate = "radiomics_features/radiomics_features.csv"
    if exists(candidate):
        feature_csv = candidate
    else:
        # 搜索项目内所有 CSV，优先选特征文件（列名含 "original_" 的是 PyRadiomics 特征）
        all_csvs = glob("**/*.csv")
        feature_csv = pick_best_match(all_csvs)

if not clinical:
    # 搜索 CSV/Excel，排除特征 CSV 和系统文件
    all_tables = glob("**/*.csv") + glob("**/*.xlsx")
    clinical = pick_clinical_table(all_tables, exclude=[feature_csv])

if not id_col:
    id_col = detect_id_column(clinical_df)    # LLM 辅助识别

if not label_col:
    label_col = detect_label_column(clinical_df)  # 找值为 0/1 的二值列
```

这是"约定优于配置"的体现——在影像组学项目中，文件位置有较强规律性（特征 CSV 在 `radiomics_features/`，临床表在项目根目录），Agent 利用这些约定减少用户的输入负担。但遇到歧义时仍会向用户确认（`need_clarification`）。

### 11.4 增量分析设计

#### 11.4.1 三种增量场景

| 场景 | 用户操作 | Agent 行为 |
|------|---------|-----------|
| **新增病例** | 把新 .nii.gz 放进 images/ + masks/，说"继续提取新病例" | discover_pairs 重新扫描 → 发现新增病例 → extract 时已有 h5 缓存跳过旧病例，只提新的 |
| **换参数重分析** | 说"把 binWidth 改成 15 重新分析" | update_yaml → 修改后 hash 变化 → 已缓存的病例自动重提 → 重新分析 |
| **已有特征 CSV，换临床表分析** | 说"用新的 clinical_v2.xlsx 做分析" | 跳过提取 → 直接用指定临床表跑分析 |

#### 11.4.2 Todo List 的状态同步

Agent 通过 `update_todo_list` 工具向用户透明地展示步骤完成情况：

```
探索后 (发现有 h5 缓存 + 特征 CSV + 分析结果):
  [✓] 项目探索
  [✓] 影像配对
  [✓] 参数确认
  [✓] 特征提取 (30/30 已完成)
  [✓] 临床表审查
  [→] 建模分析 (进行中...)
  [ ] 结果解读

只有部分病例已提取:
  [✓] 项目探索
  [✓] 影像配对
  [✓] 参数确认
  [→] 特征提取 (22/30 已完成，断点续提中...)
  [ ] 临床表审查
  [ ] 建模分析
  [ ] 结果解读
```

### 11.5 路径推断的约定体系

Agent 在文件查找和产物引用上依赖一套**松散约定**，这些约定在 `radiomics-workflow` SKILL.md 中被编码为工作流知识，而非硬编码的路径常量：

| 产物 | 默认路径 | 推断逻辑 |
|------|---------|---------|
| 提取参数 YAML | `Params_labels.yaml` | 项目根目录下的 YAML 文件 |
| 图像文件 | `images/*.nii.gz` | 递归搜索 |
| Mask 文件 | `masks/*.nii.gz` | 递归搜索 |
| 特征 CSV | `radiomics_features/radiomics_features.csv` | 列名包含 `original_` 前缀的特征文件 |
| 失败记录 | `radiomics_features/failed_cases.csv` | 与特征 CSV 同目录 |
| 每例缓存 | `radiomics_features/h5/*.h5` | FeatureAgent 固定输出 |
| 分析结果 | `radiomics_analysis/` | 包含 selected_features.csv + metrics.json + report.docx + analysis_params.json + run_analysis.py |
| 分析图表 | `radiomics_analysis/lasso/` | 逐折 LASSO path 图 |
| SHAP 可解释性 | `radiomics_analysis/shap/` | 逐折 SHAP beeswarm/bar 图 + shap_values_fold{N}.csv |
| 逐折预测 | `radiomics_analysis/predictions/` | 每折 OOF 预测概率 CSV |
| 逐折曲线 | `radiomics_analysis/curves/` | 每折 ROC/校准/DCA 图 |
| LLM 解读 | `radiomics_analysis/interpretation.md` | 三段式中文解读纯文本 |
| 统计结果 | `feature_statistics/` | 包含统计表格 .docx（docx_style 学术格式） |
| 对话导出 | `conversation_exports/` | 对话导出为 Markdown/Word 的目标目录 |
| 脚本存档 | `agent_scripts/` | `execute_python_script` 工具固定输出目录 |
| 文件操作备份 | `.onerad_backup/<timestamp>/` | `execute_plan` 自动创建 |

这套约定使 Agent 在绝大多数情况下**不需要用户逐一指定文件路径**——它自己会找到。

### 11.6 设计原则总结

| 原则 | 体现 |
|------|------|
| **先看再做** | 任何操作前先探索项目已有文件，避免盲目重跑 |
| **缓存友好** | 特征提取基于内容哈希断点续提；参数不变就不重跑 |
| **路径推断优先** | 约定 > 用户指定 > 报错。减少用户的输入负担 |
| **歧义时确认** | 当自动推断不确定（如临床表中多个候选 ID 列），用 `need_clarification` 或 `ask_user_choice` 确认 |
| **错误不扩散** | 单个病例提取失败记录到 failed_cases.csv，不影响其他病例 |
| **进度透明** | 通过 update_todo_list + radiomics_progress 向用户持续报告宏观进度和微观进度 |

---

## 12. 运行时上下文与取消机制

### 12.1 跨线程通信问题

LangGraph 的图节点（`call_llm`, `execute_confirmed` 等）是普通的同步函数，运行在 `run_in_threadpool` 的工作线程中。它们**无法直接访问** FastAPI 的 `app.state` 或 `asyncio` 事件循环。

而耗时操作需要：
1. **推送实时进度** → 需要访问 `EventBridge` 和主事件循环
2. **响应 /stop** → 需要一个线程安全的中断信号

### 12.2 AgentRunContext 注册表

```python
# app/agent/runtime.py

@dataclass
class AgentRunContext:
    cancel_event: threading.Event  # /stop 置位，耗时任务轮询
    loop: Any = None               # 主事件循环引用（用于 run_coroutine_threadsafe）
    bridge: Any = None             # EventBridge 实例

_lock = threading.Lock()
_contexts: Dict[str, AgentRunContext] = {}

def register(thread_id, loop=None, bridge=None):
    ctx = AgentRunContext(cancel_event=threading.Event(), loop=loop, bridge=bridge)
    with _lock:
        _contexts[thread_id] = ctx
    return ctx

def get(thread_id):
    with _lock:
        return _contexts.get(thread_id)

def request_cancel(thread_id):
    ctx = get(thread_id)
    if ctx is None: return False
    ctx.cancel_event.set()
    return True
```

### 12.3 进度推送的跨线程路径

```
工作线程 (execute_confirmed)                  主线程 (asyncio event loop)
        │                                              │
        ▼                                              │
_publish_agent_progress(thread_id, payload)            │
        │                                              │
        ▼                                              │
agent_runtime.get(thread_id)                           │
        │                                              │
        ├── ctx is None → 静默跳过                      │
        ├── ctx.cancel_event.is_set() → 跳过            │
        │                                              │
        ▼                                              │
asyncio.run_coroutine_threadsafe(                      │
    ctx.bridge.publish("agent", thread_id,             │
        {"radiomics_progress": payload,                │
         "running": True}),                            │
    ctx.loop                                           │
) ─────────────────────────────────────────────────▶  bridge.publish()
        │                                              │
        ▼                                              │
    返回继续提取下一个病例                               │
```

**关键保护**：`cancel_event.is_set()` 检查在 publish 之前。因为 `/stop` → `cancel_event.set()` → async task cancel 之间有时间窗口，worker 线程可能仍在推送进度——若推送 `running: True` 会在前端重新显示 busy 状态。

### 12.4 取消的协作式实现

```
/stop 端点:
  1. request_cancel(thread_id) → cancel_event.set()
     （必须优先执行：后续检查可能因 409 拒绝，但取消信号必须送达）

  2. task.cancel() → asyncio task 取消
     （取消 async generator → finally 清理 → 推送 running=false）

工作线程中的轮询:
  FeatureAgent.run(pairs, cancel_event=cancel_event, ...):
      for case in pairs:
          extract_one(case)
          if cancel_event.is_set():      # ← 协作式检查点
              return {"success": False, "cancelled": True}
          progress_callback(...)

子Agent 中的轮询:
  for values in sub_graph.stream(...):
      if cancel_event.is_set():
          cancelled = True
          break
```

**注意**：Docker/Windows 均无法安全地强杀线程。`cancel_event` 是协作式的——耗时任务必须在合适的检查点主动轮询。

### 12.5 /stop 的收尾清理

```
1. 取消 asyncio 流式任务
2. 清除 checkpoint 中残留的 interrupt_type + pending_* 字段
   （防止前端收到 running=false 后因 interrupt_type 非空而重新弹出审批面板）
3. 修复未应答的 tool_calls：
   for each missing tool_call_id:
       写入 ToolMessage({"cancelled": True, "reason": "用户停止了操作"})
   （保证下次 send_message 时 LLM 不会因 history 格式残缺而 400）
4. 补打本轮新消息的时间戳
5. 推送 running=false 的最终快照
```

---

## 13. 前端状态管理

### 13.1 Pinia Store 核心状态

```typescript
// frontend/src/stores/agent.ts - useAgentStore

// ─── 对话状态 ───
threadId: string | null           // 当前活跃对话
messages: AgentMessage[]          // 完整消息历史（由后端 values 快照全量替换）
interrupt: string | null          // 当前中断类型（驱动审批面板显示）
busy: boolean                     // 是否正在流式运行（完全由 running 字段驱动）

// ─── 中断数据 ───
pendingPlan / pendingCommand / pendingScript /
pendingRadiomicsPlan / pendingRadiomicsExecution /
pendingRadiomicsAnalysis / pendingFeatureStatistics / pendingSubagent

// ─── 实时旁路数据 ───
currentThinking: {text: string, done: boolean} | null
radiomicsProgress: {stage, current, total, patient_id} | null
subagentStatuses: Record<string, SubagentStatus>  // 多子Agent 并行展示
todos: TodoItem[]                                 // 右侧步骤面板

// ─── 对话管理 ───
threads: ThreadSummary[]          // 当前项目对话列表（扁平）
threadsByProject: Record<string, ThreadSummary[]>  // 按项目分组缓存
runningThreadIds: Set<string>     // 运行中的对话 id（侧边栏转圈）
finishedThreadIds: Set<string>    // 运行结束未读（侧边栏提示点）

// ─── 用户偏好（localStorage 持久化）──
autoApprove: boolean
selectedModel: 'deepseek-v4-flash' | 'deepseek-v4-pro'
```

### 13.2 状态同步的完整流程

```
用户发消息 → sendMessage()
  │
  ├── 乐观追加用户消息到 messages
  ├── busy = true
  ├── connect() → 开启 SSE（若未连接）
  ├── POST /threads/{id}/messages
  │   └── 后端 202 → 启动 _start_stream → graph.astream()
  │
  └── SSE 事件到达 → applyState(data)
      ├── messages 全量替换
      ├── interrupt_type → interrupt (驱动审批面板)
      ├── pending_* → 审批面板数据
      ├── thinking → currentThinking (思考链实时显示)
      ├── radiomics_progress → radiomicsProgress (进度条)
      ├── subagent → subagentStatuses (子Agent 状态卡片)
      ├── todos → todos (右侧步骤面板)
      ├── running → busy
      └── context_usage → contextUsage (token 用量指示器)

流结束 → agent_end 事件
  ├── busy = false
  ├── radiomicsProgress = null
  ├── currentThinking = null
  ├── runningThreadIds.delete(threadId)
  ├── updateRunningPolling()
  └── syncThread() → 最终 state 同步（兜底收敛）
```

### 13.3 审批流程的前端视角

```
SSE 推送 interrupt_type = "file_plan"
  → interrupt = "file_plan"
  → pendingPlan = {...}
  → busy = true (running=true)
  → 渲染 PlanEditor.vue（文件操作计划表格）

用户编辑计划 → PUT /threads/{id}/plan
  → 写回 checkpoint 的 pending_plan
  → 前端重新渲染

用户点击 [确认]
  → POST /confirm
  → busy = true
  → 后端 Command(resume={"action": "confirm"})
  → 继续 SSE 推送流
  → execute_confirmed 执行 → clear interrupt → 继续 call_llm
  → 下一轮 SSE 推送 interrupt_type = null, pendingPlan = null
  → PlanEditor 关闭

用户点击 [取消]
  → POST /cancel
  → 同上，但 execute_confirmed 走取消分支
  → cancelled ToolMessage + 可选 HumanMessage

用户点击 [其他...]
  → POST /other {instruction: "..."}
  → 同上，但带 other_instruction
```

### 13.4 轮询与运行状态收敛

```
问题: runningThreadIds 是前端内存中的状态，刷新后会丢失。
     侧边栏如何知道哪些对话正在运行？

方案: 双重来源
  1. 前端主动标记: sendMessage / confirm / cancel 后立即
     runningThreadIds.add(threadId)
  2. 后端列表响应: listThreads() 响应中每个 thread 带 running 标志
     syncRunningFromList() 合并进 runningThreadIds

     刚结束的对话: wasRunning=true 且 nowRunning=false
     → runningThreadIds.delete(id)
     → 如果用户正在看 → 不加 finishedThreadIds（已实时看到结束）
     → 否则 → finishedThreadIds.add(id)（侧边栏显示提示点）

轮询: 当 runningThreadIds 非空时，每 3 秒 pollRunningProjects()
      刷新所有含运行中对话的项目的列表
      运行中集合清空后自动停止轮询
```

---

## 14. 数据库设计

### 14.1 SQLite 表结构

```sql
-- 项目与对话在同一个 SQLite 文件（~/.onerad/projects.db 或自定义路径）
-- 由 ProjectStore 管理

CREATE TABLE projects (
    id TEXT PRIMARY KEY,          -- UUID
    name TEXT NOT NULL UNIQUE,
    path TEXT NOT NULL UNIQUE,    -- 项目根目录绝对路径
    description TEXT,
    created_at TEXT NOT NULL,     -- ISO 8601
    updated_at TEXT NOT NULL
);

CREATE TABLE threads (
    id TEXT PRIMARY KEY,          -- UUID (即 LangGraph thread_id)
    project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    title TEXT NOT NULL DEFAULT '',
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);

-- SSE 事件持久化
CREATE TABLE sse_events (
    scope TEXT NOT NULL,          -- "agent" | "pipeline"
    scope_id TEXT NOT NULL,       -- thread_id
    event_id INTEGER NOT NULL,
    data TEXT NOT NULL,           -- JSON 字符串
    created_at TEXT NOT NULL,
    PRIMARY KEY (scope, scope_id, event_id)
);

-- 项目长期记忆
CREATE TABLE memories (
    id TEXT PRIMARY KEY,          -- UUID
    project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    category TEXT NOT NULL,       -- clinical | decision | finding | preference | general
    fact TEXT NOT NULL,           -- 一句话事实
    created_at TEXT NOT NULL
);
CREATE INDEX idx_memories_project ON memories(project_id, created_at DESC);

-- LangGraph checkpoint (由 AsyncSqliteSaver 管理)
-- 内部表: checkpoint, checkpoint_blobs, checkpoint_writes
```

### 14.2 checkpoint 与 thread 的关系

- **thread 表**（ProjectStore）：存储对话元数据（标题、项目归属、时间戳）
- **checkpoint 表**（AsyncSqliteSaver）：存储 LangGraph 的完整状态快照（messages、state 字段）
- **sse_events 表**（ProjectStore）：存储 SSE 事件用于重连回放

三者通过 `thread_id` 关联。删除线程时需同时清理三处（见 `delete_thread` 端点）。

---

## 15. 项目长期记忆

### 15.1 设计动机

在影像组学分析中，用户常在同一项目下开启多个对话线程，但上下文并不互通。例如第一个对话中用户确认了"患者 ID 是 PATIENT_ID 列，Label 列是 Label（1=阳性，0=阴性）"，换到新对话后这些信息需要重新告知——既浪费上下文窗口，也降低了用户体验。

项目记忆系统在**项目粒度**上跨线程共享已知事实，实现"一次确认，项目内所有对话自动回忆"。

### 15.2 架构概要

```
对话线程 1 ── 流结束 ──▶ extract_memories() ──▶ ProjectStore.add_memories()
                                                            │
                                                  SQLite memories 表
                                                            │
对话线程 2 ── call_llm ──▶ build_memory_prompt() ◀──────────┘
                     │
                     ▼
              System Prompt 注入 "项目记忆" 块
```

### 15.3 提取（Extraction）

每次对话流正常结束（非异常、非取消）后，`_maybe_extract_memories()` 被调用：

```python
# app/api/agent.py
async def _maybe_extract_memories(thread_id, graph, config, app):
    # 1. 检查全局记忆开关
    if settings_store is not None and not settings_store.is_memory_enabled():
        return  # 功能关闭，跳过

    # 2. 用 LLM 从本轮对话中提取关键事实
    messages = _render_messages(values)  # 只取 user/assistant 消息
    memories = extract_memories(messages, llm_client)
    # 提取 prompt：只提取客观的、结论性的信息，忽略临时问答/调试/问候

    # 3. 持久化到 SQLite
    store.add_memories(project_id, memories)
    # 每条事实包含 category（clinical/decision/finding/preference/general）
    # 和 fact（一句话事实）。同 project+category+fact 去重
```

**五类记忆**：

| 类别 | 含义 | 示例 |
|------|------|------|
| `clinical` | 临床信息 | "Label 列的值 1 表示恶性，0 表示良性" |
| `decision` | 过往决策 | "用户选择使用 binWidth=15 而不是默认的 25，因为病灶较小" |
| `finding` | 分析发现 | "上一轮 LASSO 筛选出 wavelet-LLH_glcm_Contrast 为最重要特征" |
| `preference` | 用户偏好 | "用户偏好中文报告，分析方法使用五折交叉验证" |
| `general` | 其他 | "该项目使用 3T MRI 扫描，序列为 T2-FLAIR" |

### 15.4 注入（Injection）

每次 `call_llm` 节点开始前，从数据库读取记忆并注入 system prompt：

```python
# app/agent/nodes.py - call_llm
memory_block = ""
project_id = state.get("project_id", "")
if project_id:
    gs = GeneralSettingsStore()
    if gs.is_memory_enabled():
        memory_block = build_memory_prompt(project_id, ProjectStore())
# → "## 项目记忆（跨会话共享的已知事实）\n### 临床信息\n- ..."
```

`build_memory_prompt()` 按 `_CATEGORY_LABELS` 顺序组织记忆为 Markdown 格式，最多注入 20 条（`MAX_MEMORIES`），在 skills 之前插入 system prompt。

### 15.5 全局开关

用户可在**设置页面**通过开关控制记忆功能的启用/关闭：

```python
# app/settings.py - GeneralSettingsStore
def is_memory_enabled(self) -> bool:
    return self.load().get("memory_enabled", True)  # 缺省为 True
```

```yaml
# ~/.onerad/settings.yaml
features:
  memory_enabled: true   # 或 false
```

关闭后：① 新对话不再提取记忆；② 已有对话的 system prompt 中不再注入记忆。但已存储的记忆不会删除——重新开启后自动恢复。

前端 `SettingsView.vue` 通过 API `PUT /api/settings` 切换开关，`useSettingsStore` 管理状态。

### 15.6 数据库存储

```sql
-- app/projects.py - ProjectStore
CREATE TABLE IF NOT EXISTS memories (
    id TEXT PRIMARY KEY,          -- UUID
    project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    category TEXT NOT NULL,       -- clinical | decision | finding | preference | general
    fact TEXT NOT NULL,           -- 一句话事实
    created_at TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_memories_project
    ON memories(project_id, created_at DESC);
```

去重策略：`INSERT OR IGNORE`，按 `(project_id, category, fact)` 三重判重。删除项目时记忆级联删除。

### 15.7 设计原则

| 原则 | 体现 |
|------|------|
| **项目隔离** | 记忆表以 `project_id` 关联项目，不同项目间不共享 |
| **最低权限** | 开关状态仅影响提取和注入，不删除已存数据 |
| **静默失败** | 提取/注入任一步骤失败只记日志，不影响对话主流程 |
| **截断保护** | 消息过长时截断（user 1000字符/assistant 2000字符），对话只取最后 100 行，控制提取成本 |
| **用户可控** | 设置页一键开关，功能行为透明 |

---

## 16. LLM 结果解读与 SHAP 可解释性

### 16.1 设计动机

影像组学分析产出的 `metrics.json` 包含大量数值（AUC、敏感度、OR、p 值、SHAP 重要性……），这些数字对临床研究者而言不够直观。LLM 结果解读系统将这些数值转化为结构化的中文解读文本，并自动注入报告——让报告不只是"数字的堆砌"，而是"有结论的发现"。

### 16.2 三段式解读结构

系统通过 `skills/result-interpretation/SKILL.md` 中的 prompt 模板，驱动 LLM 输出三段解读：

```
【模型性能解读】
  - 判别能力（AUC + 95%CI）
  - 误差结构（混淆矩阵 → 漏诊 vs 误诊）
  - 跨折稳定性（Mean±SD 小 → 折间表现稳定）
  - 明确所有指标来自 out-of-fold 内部验证

【特征意义解读】
  - 逐一解释特征影像组学含义（特征类 + 滤波器）
  - 方向性：系数符号与 OR（OR>1 → 特征升高→阳性概率升高）
  - 与 SHAP 方向的一致性比对

【SHAP可解释性解读】
  - 按 mean|SHAP| 重要性排序解读 top 特征
  - 折覆盖次数反映跨折稳定性

三段末尾附局限性声明：相关≠因果、样本量限制、需外部验证
```

### 16.3 实现流程

```python
# app/interpretation.py

# Step 1: build_summary() — 聚合纯数值摘要
summary = build_summary(analysis_result, output_dir)
# 聚合内容：metrics、cv_metrics(逐折)、coefficients(系数/OR/CI/p)、
#           shap(跨折 mean|SHAP| + 方向 + 与系数一致性)

# Step 2: interpret() — 调 LLM 生成三段中文解读
interpretation = interpret(summary, llm_client)
# → {"performance": "…", "features": "…", "shap": "…"}
# 按 SECTION_MARKERS 分隔标记解析输出

# Step 3: apply_to_reports() — 注入报告
reports = apply_to_reports(analysis_result, output_dir, interpretation)
# 重写 report.md（追加"结果解读"小节）和 report.docx（同步更新）
# 幂等——重复调用不会叠加小节
```

`build_summary()` 的 SHAP 聚合逻辑：读取 `shap/shap_values_fold*.csv`，每特征逐折计算 mean|SHAP|，跨折平均得到全局重要性（取 top 15），折覆盖次数为该特征在几折出现，方向取逐折 mean SHAP 符号，并与回归系数方向比对给出 `direction_consistent` 标记。

### 16.4 集成到分析流程

在步骤 5（建模分析）完成后，Agent 被 skill 指引主动调用 `interpret_analysis_results` 工具：

```
run_radiomics_analysis → 生成报告 + metrics.json + SHAP CSV/png
                         │
interpret_analysis_results（免确认）
                         │
                    build_summary → interpret → apply_to_reports
                         │
                    report.md + report.docx 更新（含解读小节）
                    interpretation.md 落地（纯文本存档）
```

`interpret_analysis_results` 是**免确认工具**——在 `process_tool_calls` 中直接执行，不进入中断流程。任何失败（无 API key、LLM 异常、返回格式异常、旧输出目录缺 `analysis_result.json`）都优雅返回错误说明，不影响既有产物。

### 16.5 SHAP 可解释性分析（逐折）

在 `app/analysis.py` 的交叉验证循环中，每折训练完成后自动执行 SHAP 分析：

```python
# app/analysis.py - _save_fold_artifacts()
# 1. 选用 explainer：LinearExplainer（优先，需线性模型）→ KernelExplainer（回退）
# 2. 计算训练集 SHAP 值（取正类）
# 3. 落盘产物：
#    shap/shap_summary_fold{N}.png    — beeswarm 散点图
#    shap/shap_bar_fold{N}.png        — 柱状图（mean|SHAP|）
#    shap/shap_values_fold{N}.csv     — 每例每特征的 SHAP 值
```

SHAP 图通过 `plot_paths` 嵌入报告（报告用图：fold-1 LASSO path + 全部逐折 SHAP 图），在 report.md 中作为"## 5. SHAP 可解释性"小节。

### 16.6 报告重建的上下文恢复

`apply_to_reports()` 重新生成报告时所需的上下文（协变量、折数、特征总数、图表路径）从输出目录的磁盘产物恢复：

- `analysis_params.json` → covariates, n_splits, feature_csv
- 标准命名 PNG 文件 → roc_curve, calibration_curve, dca_curve
- `lasso/lasso_path_fold*.png` → lasso_paths
- `shap/shap_summary_fold*.png` + `shap/shap_bar_fold*.png` → shap_plots

这保证了即使不在分析刚完成的调用上下文中（例如用户稍后要求"重新解读"），也能正确重建完整报告。

---

## 17. 对话导出

### 17.1 功能概述

对话导出的核心目的是**方便用户反馈 bug**：当智能体行为异常、工具调用出错或分析结果不符合预期时，用户可将完整对话（含工具调用参数、思考链、工具返回结果）导出为 Markdown 或 Word 文档，直接发送给开发人员排查。导出的文档包含用户消息、助手回复、思考过程、每次工具调用的参数与返回值，开发人员无需复现操作即可定位问题。

导出文件写入项目目录下的 `conversation_exports/` 子目录。

### 17.2 导出格式

**Markdown**：对话呈层级结构（用户/助手为 Heading 2，工具结果为 Heading 3 + 代码块，思考过程为引用格式）。

**Word**：使用 `python-docx` 直接构建，思考过程以斜体区分，工具调用/结果以 Consolas 等宽字体呈现。

### 17.3 实现

```python
# app/conversation_export.py
def export_conversation(project_path, title, messages, fmt) -> Path:
    out_dir = Path(project_path) / "conversation_exports"
    stamp = _now_local().strftime("%Y%m%d_%H%M%S")
    stem = f"对话_{title}_{stamp}"
    if fmt == "docx":
        write_docx(title, messages, path)
    else:
        path.write_text(render_markdown(title, messages))
    return path
```

API 端点：`POST /api/agent/threads/{id}/export`，接受 `{"format": "md"|"docx"}`。

---

## 18. 临床列名自动中译英

### 18.1 问题背景

临床研究者常使用中文列名（如"年龄"、"性别"、"病理分级"）。这些列名进入分析流程后会导致：

1. **matplotlib 渲染乱码**：ROC/SHAP 图的中文标注在无中文字体的环境中显示为方块
2. **报告可读性**：英文报告中出现中文别名不一致
3. **复现性**：中文路径/列名在不同操作系统间行为不一致

### 18.2 翻译机制

```python
# app/clinical.py
def translate_column_names(names: List[str], llm_client) -> Dict[str, str]:
    """用一次 LLM 调用把中文等非 ASCII 临床列名翻译为医学通用英文。
    仅保留纯 ASCII、非空的译名；非法译名不出现在结果中（调用方对其保持原名）。
    """
```

在 `run_radiomics_cv_analysis()` 中，合并特征与临床数据后，自动检测非 ASCII 列名并翻译：

```python
# 首次运行
non_ascii_cols = [c for c in merged_df.columns if not str(c).isascii()]
if non_ascii_cols and llm_client:
    translation_mapping = translate_column_names(non_ascii_cols, llm_client)
    merged_df = merged_df.rename(columns=translation_mapping)
    # 同步更新 id_col、label_col 映射

# 复跑路径：column_name_mapping 非空时直接查表重命名，不调 LLM
if column_name_mapping:
    clinical_df = clinical_df.rename(columns=column_name_mapping)
```

### 18.3 复现保证

翻译映射写入 `analysis_params.json` 的 `column_name_mapping` 字段。复跑时按映射直接重命名临床列，不调用 LLM，保证复现的确定性。

报告中协变量以双语对照形式展示：`Age（年龄）`（通过反向映射 `{英文: 中文}` 查找）。

---
## 19. 关键设计决策与权衡



| 决策 | 选择 | 替代方案 | 理由与权衡 |
|------|------|---------|-----------|
| Agent 框架 | **LangGraph StateGraph** | 自建状态机 / LangChain AgentExecutor | `interrupt()` 原生支持人机协同；`AsyncSqliteSaver` 提供生产级 checkpoint；状态图显式建模比 AgentExecutor 的隐式循环更可控 |
| LLM 调用方式 | **原生 OpenAI SDK** | LangChain ChatOpenAI | DeepSeek 的 `reasoning_content` 是 OpenAI 协议的非标准扩展。LangChain 的流式拼接逻辑不认识此字段，直接丢弃。原生 SDK 保留完整控制 |
| LLM 模型 | **DeepSeek V4** | OpenAI GPT-4o / Claude | 成本（DeepSeek 便宜 10-20x）、中文推理能力、`reasoning_content` 思考链可见性 |
| API Key 存储 | **RunnableConfig 内存** | 写入 AgentState | 防止密钥随 checkpoint 持久化到 SQLite。删除线程时同步从内存清除 |
| Skill 加载 | **每次从磁盘读取** | 启动时缓存 / 写入代码常量 | 热更新——调整 prompt 后立即生效无需重启。磁盘 IO 开销可忽略（每个 LLM 调用读一次 ~3KB 文件 vs 数秒的 API 调用） |
| 子Agent 嵌套深度 | **限制为 1** | 无限制 / 递归 | 一层分派已覆盖主场景（项目探索 + 复杂计算子任务）；更深嵌套增加调试难度且收益递减 |
| 并行子Agent 并发 | **最多 4 worker** | 不限制 | 平衡 DeepSeek API 速率限制（同一 key 的并发请求）、本地内存占用、实际加速比（项目探索场景 3-4 个任务最优） |
| 子Agent 步数上限 | **150 superstep (~37 轮工具调用)** | 默认 25 | 子Agent 的整个生命周期在单次 stream 内完成，无分段预算；37 轮足以完成复杂的数据探索和分析任务 |
| 子Agent 结果截断 | **4000 字符** | 不截断 / 更短 | 4000 字符足以传达结论、文件路径和关键数字；过短丢失信息，过长消耗主对话上下文 |
| 同一轮 tool_calls 限制 | **最多 1 个需确认** | 允许多个 | 防止 `pending_*` 互相覆盖（状态字段非列表，只有一份）。多个需确认工具 LLM 可串行分轮发送 |
| high 风险脚本 | **标记高危 + 用户知情确认后执行** | 直接拒绝 | 把决策权交回用户。合法任务（如 `requests` 下载数据集）不应被技术手段阻止 |
| 中危脚本沙箱 | **注入沙箱头 wrapper** | 静态拒绝 / Docker | 运行时 hook `open()` 是 best-effort 防御，不是安全边界。在 windows 上比 Docker 更轻量且无额外依赖 |
| SSE 高频事件 | **不持久化** | 全部持久化 | 避免 SQLite 写入成为瓶颈（thinking 每秒可产数十 chunk）。重连靠 values 快照兜底（完整思考链在 AIMessage.additional_kwargs 中） |
| `parallel_tool_calls` | **关闭 (False)** | 开启 | 杜绝单轮内多个需确认 tool_calls 的覆盖问题；也降低单轮 token 峰值和后续处理复杂度 |
| 文件操作备份 | **执行前自动备份到 `.onerad_backup/`** | 不做备份 | move/copy 的覆盖操作不可逆；时间戳子目录实现简单快照 |
| 特征提取断点续提 | **基于 h5 缓存 + YAML hash 的断点续提** | 每次全量重跑 | 30 个病例提取可能需 10-30 分钟；中途失败不应从头开始 |
| 前端 busy 状态 | **完全由后端 `running` 字段驱动** | 由 `interrupt_type` 推断 | `execute_confirmed` 清除 pending 前的中间快照仍带旧 interrupt_type，用其推断 busy 会导致 UI 闪烁 |
| 前端乐观更新 | **用户消息乐观追加到 messages，失败回滚** | 等后端确认后再显示 | 消息发送延迟需数百 ms；乐观更新消除感知延迟。冲突由后端的 409（流忙碌/待确认）兜底 |

---

## 20. 完整 API 参考

### 20.1 Agent 端点

| 方法 | 路径 | 说明 | 响应 |
|------|------|------|------|
| `POST` | `/api/agent/threads?project_id=` | 创建新对话 | `201 {"thread_id": "uuid"}` |
| `GET` | `/api/agent/threads?project_id=` | 列出项目的对话 | `200 {"threads": [...]}` |
| `GET` | `/api/agent/threads/{id}` | 获取对话状态 | `200 AgentState` |
| `PATCH` | `/api/agent/threads/{id}` | 重命名对话 | `200 {"thread": {...}}` |
| `DELETE` | `/api/agent/threads/{id}` | 删除对话（含所有 checkpoints/events） | `204` |
| `POST` | `/api/agent/threads/{id}/resume` | 恢复对话（刷新 API Key） | `200 AgentState` |
| `POST` | `/api/agent/threads/{id}/messages` | 发送消息（启动流式运行） | `202 {"thread_id": "uuid"}` |
| `PUT` | `/api/agent/threads/{id}/plan` | 编辑文件操作计划 | `200 AgentState` |
| `POST` | `/api/agent/threads/{id}/confirm` | 确认当前中断 | `202 {"thread_id": "uuid"}` |
| `POST` | `/api/agent/threads/{id}/cancel` | 取消当前中断 | `202 {"thread_id": "uuid"}` |
| `POST` | `/api/agent/threads/{id}/other` | 取消中断 + 传替代指令 | `202 {"thread_id": "uuid"}` |
| `POST` | `/api/agent/threads/{id}/stop` | 停止运行中的流（不清除对话） | `202 {"thread_id": "uuid", "status": "stopped"}` |
| `PUT` | `/api/agent/threads/{id}/auto-approve` | 切换自动审批 | `200 {"auto_approve": bool}` |
| `POST` | `/api/agent/threads/{id}/export` | 导出对话（md/docx） | `200 {"path": "...", "format": "md"}` |
| `GET` | `/api/agent/threads/{id}/events` | SSE 事件流 | `text/event-stream` |

### 20.2 并发保护

| 场景 | HTTP 状态码 | 说明 |
|------|-----------|------|
| 线程已有流在运行中，再次 `sendMessage` | `409` | "智能体正在处理上一条消息" |
| 线程在中断等待确认中，`sendMessage` | `409` | "当前存在待确认的操作" |
| `sendMessage` 时线程已在运行中（`_start_stream` 原子检查） | `409` | "智能体正在处理中" |
| `stop` 时没有运行中的任务 | `409` | "当前没有正在运行的任务" |
| `other` 时没有待确认的操作 | `409` | "当前没有待确认的操作" |

---

## 21. 附录

### 21.1 interrupt_type 完整矩阵

| interrupt_type | 触发工具 | pending 字段 | 前端面板 | 用户可编辑 | 取消时追加 HumanMessage |
|---------------|---------|-------------|---------|:---:|:---:|
| `system_command` | list_directory, find_files, get_file_info, read_yaml, read_json, read_tabular_file, update_yaml, create_json, update_json, inspect_image_spacing, word_create, word_append | `pending_command` | CommandPanel | — | ❌ |
| `file_plan` | plan_file_operations | `pending_plan` | PlanEditor | ✅ 编辑计划 | ✅ |
| `python_script` | execute_python_script | `pending_script` | ScriptPanel | — | ✅ |
| `radiomics_plan` | discover_radiomics_pairs | `pending_radiomics_plan` | RadiomicsPanel | ✅ 修改配对 | ✅ |
| `radiomics_execution` | extract_radiomics_features | `pending_radiomics_execution` | RadiomicsPanel | — | ✅ |
| `radiomics_analysis` | run_radiomics_analysis | `pending_radiomics_analysis` | RadiomicsPanel | — | ✅ |
| `feature_statistics` | run_feature_statistics | `pending_feature_statistics` | RadiomicsPanel | — | ✅ |
| `subagent_dispatch` | dispatch_subagent(general) | `pending_subagent` | ApprovalPanel | — | ✅ |

### 21.2 核心文件索引

| 文件路径 | 职责 |
|---------|------|
| `app/agent/__init__.py` | 图创建入口、`build_initial_state()` |
| `app/agent/graph.py` | LangGraph 图结构定义（5 节点 + 2 条件边） |
| `app/agent/state.py` | `AgentState` TypedDict 定义 |
| `app/agent/nodes.py` | 5 个节点函数 + 子Agent 执行 + 影像组学/分析/统计执行 + 思考链推送 |
| `app/agent/tools.py` | ~23 个工具定义 + `build_tools()` 注册函数 |
| `app/agent/safety.py` | `Sandbox` 路径沙箱 + `validate_plan()` |
| `app/agent/runtime.py` | `AgentRunContext` 注册表（取消事件 + 线程间通信） |
| `app/agent/memory.py` | 项目长期记忆：`extract_memories()` 提取 + `build_memory_prompt()` 注入 |
| `app/api/agent.py` | REST API 端点（线程 CRUD、消息、确认/取消/停止/SSE、对话导出） |
| `app/api/sse.py` | `EventBridge` 发布/订阅总线 + 进度补偿 |
| `app/api/runner.py` | `get_bridge()` 从 app.state 获取 EventBridge |
| `app/skills.py` | Skill 从磁盘加载 + `load_skill_bundle()` |
| `app/settings.py` | `GeneralSettingsStore`：全局设置（API Key、记忆开关）YAML 持久化 |
| `app/code_runner.py` | Python 脚本风险分级 + 沙箱头注入 + 执行 |
| `app/actions.py` | 文件操作计划执行（move/copy/rename/mkdir + 自动备份） |
| `app/radiomics_discovery.py` | 图像/mask 自动配对发现 |
| `app/radiomics_analysis.py` | LASSO + 逻辑回归分析 + `inspect_analysis_inputs()` + 复跑脚本生成 |
| `app/feature_statistics.py` | 特征分组统计（t 检验 + MWU）+ docx_style 学术格式报告 |
| `app/feature.py` | `FeatureAgent` 批量 PyRadiomics 特征提取（含断点续提） |
| `app/analysis.py` | 交叉验证引擎：逐折 LASSO 筛选 + 逻辑回归 + SHAP 可解释性分析 |
| `app/interpretation.py` | LLM 结果解读：`build_summary()` 聚合 → `interpret()` 调 LLM → `apply_to_reports()` 注入 |
| `app/clinical.py` | 临床表列名智能识别 + 中文列名 LLM 翻译为英文 |
| `app/report.py` | `ReportAgent`：生成中文学术格式 Word 报告（含解读注入） |
| `app/curves.py` | ROC/校准/DCA 曲线 + SHAP beeswarm/bar 图绘制 |
| `app/word_document.py` | Agent 可用的 Word 文档操作：创建/追加/重排 |
| `app/docx_style.py` | 中文学术论文 docx 排版规范（宋体小四/黑体标题/1.5倍行距） |
| `app/conversation_export.py` | 对话导出为 Markdown/Word 文档 |
| `app/projects.py` | `ProjectStore` SQLite 管理（项目/线程/记忆/SSE 事件表） |
| `app/llm.py` | LLM 客户端封装 + 标题生成 + JSON 模式调用 |
| `app/constants.py` | 模型名常量 |
| `skills/agent-core/SKILL.md` | Agent 核心行为 System Prompt |
| `skills/radiomics-workflow/SKILL.md` | 影像组学工作流 System Prompt |
| `skills/word-report/SKILL.md` | Word 文档格式规范 + word_create/word_append/reformat_report 使用指引 |
| `skills/result-interpretation/SKILL.md` | 结果解读三段式 prompt（性能/特征/SHAP） |
| `skills/clinical-columns/SKILL.md` | 临床表列名识别：ID/Label/协变量 |
| `skills/report-writing/SKILL.md` | Word 报告方法学描述润色 |
| `skills/thread-title/SKILL.md` | 对话标题生成 |
| `frontend/src/stores/agent.ts` | Pinia Agent Store（状态管理 + API 调用 + SSE 消费） |
| `frontend/src/api/agent.ts` | 前端 Agent API 客户端 + TypeScript 类型 + SSE 连接 |
| `frontend/src/api/settings.ts` | 前端设置 API（API Key/记忆开关/模型选择） |
| `frontend/src/stores/settings.ts` | Pinia Settings Store（全局设置状态管理） |
| `frontend/src/views/SettingsView.vue` | 设置页面（API Key 配置 + 长期记忆开关） |
| `frontend/src/components/AgentChat.vue` | 主聊天界面 |
| `frontend/src/components/ApprovalPanel.vue` | 通用审批面板 |
| `frontend/src/components/PlanEditor.vue` | 文件操作计划编辑器 |
| `frontend/src/stores/agent.ts` | Pinia Agent Store（状态管理 + API 调用 + SSE 消费） |
| `frontend/src/api/agent.ts` | 前端 Agent API 客户端 + TypeScript 类型 + SSE 连接 |
| `frontend/src/components/AgentChat.vue` | 主聊天界面 |
| `frontend/src/components/ApprovalPanel.vue` | 通用审批面板 |
| `frontend/src/components/PlanEditor.vue` | 文件操作计划编辑器 |
| `frontend/src/components/ScriptPanel.vue` | 脚本审批面板 |
| `frontend/src/components/RadiomicsPanel.vue` | 影像组学审批面板 |
| `frontend/src/components/TodoPanel.vue` | 右侧步骤进度面板 |

### 21.3 向后兼容约定

- `AgentState.api_key` 标记为 `NotRequired`：新线程不再写入此字段，但旧 checkpoint 可能仍有值。`_resolve_api_key` 的优先级：`configurable` → `state` → 环境变量
- `AgentState.model` 写入受支持模型名；`_resolve_model` 对不在 `DEEPSEEK_MODELS` 中的值回退到默认模型（`deepseek-v4-flash`）
- 取消操作时的 `HumanMessage` 追加只针对重操作类型（`file_plan`/`python_script`/`radiomics_*`/`feature_statistics`/`subagent_dispatch`），简单查询不追加
- `executed: True` 注入：对 dict 结果做键注入保留原结构，对非 dict 结果包裹为 `{"executed": True, "results": ...}`
- `/stop` 的 checkpoint 清理：清除残留 `interrupt_type` 后，若 snapshot 仍含未应答 tool_calls，为每个缺失 id 补 ToolMessage（保证后续 LLM 调用不 400）

### 21.4 术语对照

| 中文 | 英文（代码中） | 说明 |
|------|--------------|------|
| 对话/线程 | thread | LangGraph 的一个 checkpoint 会话 |
| 中断 | interrupt | LangGraph `interrupt()` 挂起图执行 |
| 工具调用 | tool_call | LLM function calling 的一次调用 |
| 子Agent | subagent | `dispatch_subagent` 分派的独立执行单元 |
| 审批面板 | ApprovalPanel | 中断时前端展示的确认界面 |
| 思考链 | thinking / reasoning_content | DeepSeek 推理模型的思考过程 |
| 断点续提 | resume | 特征提取中已完成的 h5 缓存跳过重提 |
| 自动审批 | auto_approve | 跳过 human_review，直接执行 |
